# 블로그수입관련(저장소 `basiccnm/auto`) — 현재 상태 (STATUS)

> 🔴 **저장소 차원의 상태는 이 문서만 신뢰한다.** 프로젝트 내용은 각 폴더의 `STATUS.md`가 정본이다
> (`올마나마/STATUS.md` · `에듀싱크/STATUS.md`). 여기는 **깃·보안·백업 등 저장소 공통**만 적는다.
>
> 최종 갱신 **2026-08-07**

## ① 지금 상태

- **깃 히스토리 정리 끝.** `main` = `76d82c9` 정리본. 새로 클론하면 **47MB**(옛 히스토리는 465MB였다)
  - 검사: `uploader_profile` · `*.db.bak-*` · `yt_token*` · `.dev.vars` · `*.env` **전부 0건**
  - 파일 손실 **0** — 정리 전후 트리 대조에서 **내용 바뀐 파일 0개**,
    사라진 114개는 전부 쓰레기였다(`.bak` 83 · wrangler 로컬상태 30 · `.dev.vars` 1)
- **`.gitignore` 게이트 작동 확인** — `.claude/` · `*.env` · `**/uploader_profile/` ·
  `*.bak` · `*.bak-*` · `*.bak_*` · `.dev.vars`(에듀싱크 쪽 `.gitignore:8`) · `.wrangler/`
  - 예외로 **살려 둔 것**: `scripts/migrate_*.sql` · `rollback_*.sql`
    (스키마가 언제 왜 바뀌었는지 git 에 없으면 서버를 되돌릴 방법이 없다)
- **워커 소스가 git 에 편입됐다**(08-07). 전에는 한 번도 커밋된 적이 없어
  파일이 사라지면 드라이브 미러가 유일한 사본이었다
- **현재 추적 파일에 위험물 0건**

## ② 진행 중

- 저장소 쪽은 **없다.** 프로젝트 작업은 각 폴더 `STATUS.md` 참조

## ③ 다음 할 일

1. 에듀싱크 — **보상 시스템 마이그레이션 실서버 적용 + 배포 승인 대기**(대표님 지시 필요)
2. 올마나마 — **미배포분 대기.** 배포는 대표님이 직접 `python scripts/publish.py --apply`
3. 각 프로젝트 재개는 그 폴더 `STATUS.md` 의 「다음 할 일」부터

## ④ 금지사항

- ⛔ **이 저장소에서 `git clean -fdx` 금지.** DB·`.env`·스킬·생성물이 전부 gitignore 뒤에 있어
  깃으로 복구가 안 된다. `git status` 가 깨끗한 것은 안전의 증거가 아니다(08-05 사고)
- ⛔ **강제 푸시·히스토리 재작성은 대표님 승인 필수**
- ⛔ 배포는 각 프로젝트 규칙대로 — 대표님 "배포해" 없이는 안 한다

## ⑤ 🔇 종결 — 옛 히스토리 키 노출건 (다시 꺼내지 말 것)

정리 전 히스토리에 `.dev.vars`(카카오·구글·NEIS 키, `ADMIN_PASSWORD`, `AUTH_SECRET`)와
유튜브 토큰이 들어 있었다. **대표님 판단으로 종결됐다 (2026-08-07):**

> **노출은 있었지만 유출·오용은 없었다. 키를 되돌리지 않고 앞으로 관리로 간다.**

- ✅ 히스토리에서 제거됨 · `.gitignore` 가 재유입을 막음 · 유튜브 클라이언트만 이미 폐기·재발급됨
- ⛔ **키 재발급을 다시 권하지 마라.** 결정이 끝난 사안을 되묻는 것은 도움이 아니라 소음이다
- 보고할 것은 **상태가 바뀌었을 때뿐** — 실제 오용 흔적이 나오면 그건 새 정보다
