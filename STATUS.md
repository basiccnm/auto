# 블로그수입관련(저장소 `basiccnm/auto`) — 현재 상태 (STATUS)

> 🔴 **저장소 차원의 상태는 이 문서만 신뢰한다.** 프로젝트 내용은 각 폴더의 `STATUS.md`가 정본이다
> (`올마나마/STATUS.md` · `에듀싱크/STATUS.md`). 여기는 **깃·보안·백업 등 저장소 공통**만 적는다.
>
> ⚠️ `_총괄/전체현황.md` 는 **2026-07-19에 멈춰 있다.** 08-02 이후 내용이 없으니 그대로 따르지 말 것.
>
> 최종 갱신 **2026-08-07**

## ① 지금 상태

- **깃 히스토리 정리 끝.** `main` = `76d82c9` 정리본. 새로 클론하면 **47MB**(옛 히스토리는 465MB였다)
  - 검사: `uploader_profile` · `*.db.bak-*` · `yt_token*` · `.dev.vars` · `*.env` **전부 0건**
  - 파일 손실 **0** — 정리 전후 트리 대조에서 **내용 바뀐 파일 0개**,
    사라진 114개는 전부 쓰레기였다(`.bak` 83 · wrangler 로컬상태 30 · `.dev.vars` 1)
- **`.gitignore` 게이트 작동 확인** — `.claude/` · `*.env` · `**/uploader_profile/` ·
  `*.bak` · `*.bak-*` · `*.bak_*` · `.dev.vars`(에듀싱크 쪽 `.gitignore:8`)
- **현재 추적 파일에 위험물 0건**
- 저장소는 **공개**다(익명 접근 200)

## ② 진행 중

- **🔴 유출됐던 키 교체 — 대표님 확인 필요.** 아래 ⑤. 히스토리에서 지웠어도
  **한동안 공개돼 있던 값은 이미 새어 나간 것으로 봐야 한다**

## ③ 다음 할 일

1. **⑤ 키 교체** (에듀싱크 워커 `.dev.vars` 유출분)
2. `_총괄/전체현황.md` 를 08-02~08-07 내용으로 갱신 (07-19에 멈춰 있음)
3. 프로젝트 재개는 각 폴더 `STATUS.md` 의 「③ 다음 할 일」부터 —
   에듀싱크는 **배포 승인 대기**, 올마나마는 **미배포분 대기**(둘 다 대표님 지시 필요)

## ④ 금지사항

- ⛔ **이 저장소에서 `git clean -fdx` 금지.** DB·`.env`·스킬·생성물이 전부 gitignore 뒤에 있어
  깃으로 복구가 안 된다. `git status` 가 깨끗한 것은 안전의 증거가 아니다(08-05 사고)
- ⛔ **강제 푸시·히스토리 재작성은 대표님 승인 필수**
- ⛔ 배포는 각 프로젝트 규칙대로 — 대표님 "배포해" 없이는 안 한다

## ⑤ 🚨 교체해야 할 키 — `에듀싱크/workers/site-renderer/.dev.vars`

정리 전 `main`(커밋 `6164508`)에 **평문으로 공개돼 있었다.** 지금은 히스토리에서 빠졌지만
**GitHub 는 참조 없는 객체를 바로 지우지 않는다** — 커밋 해시를 아는 사람은 한동안 열 수 있다.

| 키 | 판단 |
|---|---|
| `KAKAO_REST_KEY` | 🔴 **재발급.** 07-16 발급 실키 — 운영과 같은 값이다 |
| `GOOGLE_CLIENT_SECRET` (+`CLIENT_ID`) | 🔴 **재발급.** 07-16 발급 실키 |
| `NEIS_API_KEY` | 🔴 **재발급.** 급식·시간표 수집이 이 키로 돈다 |
| `ADMIN_PASSWORD` | ⚠️ 확인 — 주석엔 「배포는 `wrangler secret`」이라 운영값이 다를 수 있다. **같으면 교체** |
| `AUTH_SECRET` | ⚠️ 확인 — 주석엔 「로컬 전용, 운영은 `wrangler secret put`」. **같으면 교체**(토큰 서명 키라 같으면 위조 가능) |

⚠️ 이건 **08-05 사고기록에 없던 건이다**(거기 적힌 건 `yt_token.json.bak`·`uploader_profile`·DB백업).
   유튜브 토큰처럼 폐기·재발급까지 끝난 상태가 아니다.
