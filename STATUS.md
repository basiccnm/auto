# 블로그수입관련(저장소 `basiccnm/auto`) — 현재 상태 (STATUS)

> 🔴 **저장소 차원의 상태는 이 문서만 신뢰한다.** 프로젝트 내용은 각 폴더의 `STATUS.md`가 정본이다
> (`올마나마/STATUS.md` · `에듀싱크/STATUS.md`). 여기는 **깃·보안·백업 등 저장소 공통**만 적는다.
>
> ⚠️ `_총괄/전체현황.md` 는 **2026-07-19에 멈춰 있다.** 08-02 이후 내용이 없으니 그대로 따르지 말 것.
>
> 최종 갱신 **2026-08-07** (마지막 커밋 `76d82c9` 09:39 기준)

## ① 지금 상태

- **작업 브랜치 `claude/status-md-reading-gkoeee` (12커밋) = 정리된 히스토리.**
  `filter-repo` 뒤의 결과물이다. 도달 가능 객체에 `uploader_profile`·`*.db.bak-*`·`yt_token*` **0건**
- **🔴 `main`(6커밋)은 정리 전 히스토리 그대로다.** 두 히스토리는 **공통조상이 없다**(`merge-base` 없음)
  — 즉 브랜치를 머지해도 main 의 옛 히스토리는 안 사라진다. 클론이 **492MB** 인 원인이 이것
  | main 히스토리에 남은 것 | 수량 | 위험도 |
  |---|---:|---|
  | `올마나마/_shorts/uploader/uploader_profile/` | 1,186 파일 | 낮음 — 캐시·확장·모델 파일. **쿠키·Login Data 는 커밋 안 됐다**(확인함). `Local State` 1개뿐이고 그 키는 그 PC DPAPI 로만 풀린다 |
  | `올마나마/data/*.db.bak-*` | 35개 | 중간 — 사업 데이터 |
  | `yt_token.json.bak` (`client_secret`+`refresh_token`) | 1개 | **폐기 완료** — 콘솔에서 클라이언트 삭제·재발급함(08-07) |
- 저장소는 **공개**다(익명 접근 200). 위 표는 지금도 누구나 받아 갈 수 있다는 뜻
- `.gitignore` 게이트는 들어갔다 — `.claude/` · `*.env` · `**/uploader_profile/` · `*.bak` · `*.bak-*` · `*.bak_*`
- **현재 추적 파일에는 위험물 0건**(`.bak`·`.env`·토큰·프로필·DB 전부 없음)

## ② 진행 중

- **`main` 히스토리 정리 — 대표님 승인 대기.** 파괴적(강제 푸시)이라 지시 없이는 안 한다

## ③ 다음 할 일

1. **판단 필요 — `main` 을 어떻게 할지** (아래 ⑤)
2. `_총괄/전체현황.md` 를 08-02~08-07 내용으로 갱신 (07-19에 멈춰 있음)
3. 프로젝트 재개는 각 폴더 `STATUS.md` 의 「③ 다음 할 일」부터 —
   에듀싱크는 **배포 승인 대기**, 올마나마는 **미배포분 대기**(둘 다 대표님 지시 필요)

## ④ 금지사항

- ⛔ **이 저장소에서 `git clean -fdx` 금지.** DB·`.env`·스킬·생성물이 전부 gitignore 뒤에 있어
  깃으로 복구가 안 된다. `git status` 가 깨끗한 것은 안전의 증거가 아니다(08-05 사고)
- ⛔ **강제 푸시·히스토리 재작성은 대표님 승인 필수**
- ⛔ 배포는 각 프로젝트 규칙대로 — 대표님 "배포해" 없이는 안 한다

## ⑤ 🚨 결정 대기 — `main` 정리 방법

| 안 | 하는 일 | 결과 |
|---|---|---|
| A | 정리된 브랜치를 **`main` 으로 강제 교체** | 옛 히스토리 참조 끊김. 저장소 492MB → 19MB. **커밋 해시가 전부 바뀐다** |
| B | 그대로 두고 앞으로만 관리 | 옛 히스토리·DB백업 35개가 공개로 계속 남는다 |

⚠️ A 를 해도 **GitHub 는 옛 객체를 바로 안 지운다** — 커밋 URL 을 직접 알면 한동안 열린다.
   완전 제거는 GitHub 지원에 GC 요청이 필요하다.
