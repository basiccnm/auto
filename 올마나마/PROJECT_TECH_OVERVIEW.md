# 얼마남아 (음식 원가 계산기) — 기술 개요 / 진행상황

> B2C: 프랜차이즈 메뉴 "소매가 기준 재료비" 콘텐츠 + 애드센스. 학원비사이트 스택 복제.
> 설계 확정: Obsidian `사업관리/05_얼마남아(음식원가계산기)/B2C/00_기획_확정사항.md`
> 최종 업데이트: 2026-07-16

## 스택
- Cloudflare Workers (site-renderer) + D1. 학원비사이트(academy) 템플릿 복제.
- 정적 데이터는 Python ETL로 D1 시딩. 시세는 매일 크론(collect_prices).

## 정보구조 (3단 계층)
`/{category}/` → `/{category}/{brand}/` → `/{category}/{brand}/{menu}/`
카테고리 → 브랜드 → 메뉴. 각 계층이 SEO 유입 페이지.

## DB (schema.sql — 검증완료)
categories → brands → menus → menu_ingredients ← ingredients → price_history + banners

## 데이터 소스 (전부 해결됨 2026-07-16)
- **KAMIS**(채소·과일·곡물+소·돼지·계란·우유): 키 `638151fe-...` cert_id=dndmotor1@gmail.com ✅
  - `dailyPriceByCategoryList` / ⚠️User-Agent 필수 / http만 / p_product_cls_code=01(소매) 02(도매)
- **축평원**(닭): 키 `01d41ee3...` ✅ **닭 해결**
  - `http://data.ekape.or.kr/openapi-data/service/user/grade/consumerPriceDaily?serviceKey=&standYmd=YYYYMMDD&judgeKind=9901`
  - 축종코드: 4301소/4304돼지/4401수입소/4402수입돼지/9901닭/9903계란/9908우유. 응답 ntslPrc(평균가)
- **참가격**(가공식품): 키 `01d41ee3...`(동일), `http://openapi.price.go.kr/openApiImpl/ProductPriceInfoService/getProductPriceInfoSvc.do?ServiceKey=&goodInspectDay=YYYYMMDD`
  - ⏳ 키가 참가격 서버로 동기화 대기중(하루). PDF FAQ 확인: 키·주소 다 맞음, 시간만 필요
- 쿠팡: 파트너스 배너(iframe coupa.ng/cn579f) + 재료명 복사버튼

## ✅ 완료 (2026-07-16)
- schema.sql (7테이블) 검증
- data/seed_ingredients.sql — 재료 52종 (live 13 / composite 1 / manual 38). gen_ingredients_seed.py
- data/seed_brands.sql — 카테고리 8 + 브랜드 95 (홈페이지 있는 것만). gen_brands_seed.py
  - 슬러그=홈페이지 도메인 추출, 충돌 처리됨
- scripts/collect_prices.py — KAMIS 실시세 수집 **라이브 작동** (14재료 price_history 저장 확인)
- data/eolmanama.db — 로컬 DB (재료+브랜드+시세 들어있음)

## ✅ 소스·양념 배합 메뉴별 정확화 (2026-07-16)
- **문제**: 소스 배합이 재료키(sauce_normal 등 5종)에 통짜 하드코딩 → 로제·크림·짜장·양념이 전부 "토마토케첩·고추장…" 등 동일 문구로 부정확 (소스 포함 250메뉴/371)
- **해결**: `menu_ingredients.composition` 컬럼 신설, 메뉴 250개를 요리별 실제 배합으로 개별 큐레이션
  - `scripts/sauce_data.py` — MENU_COMP(menu_id→배합) + MID_OVERRIDE(소스 2줄 메뉴). 예: 빅맥=빅맥소스, 뿌링클=스노윙치즈시즈닝, 교촌허니=아카시아꿀간장, 로제=토마토+생크림, 초밥=간장·고추냉이
  - `scripts/apply_sauce.py` — composition 주입 (배합 254줄 / 의도적숨김 5줄: 한솥 종합도시락·도가니탕2줄째·음료)
  - `gen_preview_html.py` — SAUCE_COMP 하드코딩 제거, composition 기반 렌더. parse_comp로 명칭/재료/부가설명 분리. 배합 없으면 일반 재료행으로(합계 유지)
  - `scripts/gen_sauce_review.py` → `_preview/_sauce_review.html` — 전수 검수 페이지(카테고리별 250메뉴+배합, 숨김 표시)
- ⏳ **사용자 전수 검수 대기**: _sauce_review.html 보고 틀린 메뉴 번호+올바른 배합 알려주면 sauce_data.py 수정 후 재적용

## ✅ 메뉴 재료 전수 감사·교정 (2026-07-16)
- **배경**: 메뉴→재료 매핑이 Gemini 자동생성이라 오류 다수(예: 소대창→돼지등심, 순대→중식생면, 두부포케에 두부 없음)
- **방법**: 13개 카테고리를 병렬 서브에이전트로 전수 검증(모르는 메뉴는 웹검색). 결과 JSON을 취합해 mid 단위로 교정.
  - 스크립트: `fetch`용 scratchpad/corrections_*.json → `fix_ingredients_full.py`(REPLACE 49·ADD 22·DELETE 7·NEW재료 14) → `compute_line_costs.py`
- **주요 교정 유형**: 축종오류(우삼겹판다 돼지→소, 소고기쌀국수 돼지→양지), 곱창류→돼지등심, 메인누락(한우곱창구이·두부포케·감자튀김·순대·피자토핑), 쌀국수 면(중식면→쌀국수), 명랑핫도그 빵(버거번→핫도그빵), 크림/연유→우유, 중복행 정리
- **NEW 재료 14종**: 새우살·닭가슴살·닭발(뼈있는)·당면·도가니·메밀면·민트시럽·삶은순대·소고기큐브스테이크·쌀떡·오소리감투·치즈시즈닝·페퍼로니·감자
- **검수 페이지**: `_preview/_menu_audit.html`(371개 전 메뉴 재료+플래그) — 현재 🔴0 🟠0. `scripts/gen_menu_audit.py`
- ⏳ **저신뢰 항목(사용자/매장 확인 권장)**: 큐브스테이크덮밥(소↔돼지), 청년피자 에그머니나(계란 추정), 막창덮밥(소↔돼지막창), 제육 부위(등심↔전지). 경미: 시래기 generic채소, 크림치즈 부재(모짜 대용), milk/milk_fresh·coffee_bean/coffee_beans 중복키

## ✅ 소스 → 시판 대체제(쿠팡 판매 제품) 적용 (2026-07-16)
- **원칙(사용자 확정)**: 시판 대체제가 **있으면 그 제품으로 올리고, 없으면 안 올림**(배합만 유지). 시판소스 우선, 밀키트도 가능.
- **효과**: ①원가가 실제 소매가 ②재료명 복사 → **쿠팡 파트너스 구매 전환** ③독자는 "사서 뿌리면 끝"
- `scripts/apply_retail_sauces.py` — 시판제품 23종 등록(`ingredients.is_retail=1`) + composition 규칙 매칭으로 소스행 remap
  - **결과: 109줄 시판제품 / 143줄 배합만(대체제 없음)**
  - 예: 오뚜기 양념치킨소스490g(10,200/kg)·아이엠소스 뿌링클링시즈닝500g(17,500)·청정원 로제스파게티소스600g(9,970)·하오츠 마라소스180g(22,200)·청정원 춘장250g(14,000)·오뚜기 돈까스소스470g(5,200)·하인즈 버거소스230g(10,000) 등
  - **미적용(대체제 없음)**: 짬뽕·간장/마늘간장치킨·허니·갈릭·족발조림·국밥다대기·반미·초밥곁들임·소금구이 등 → 기존 배합 유지
- 렌더: `is_retail`이면 "🛒 시판 소스로 간편하게 → [제품명(복사)]" + "집에서 직접 만들 땐 → [배합]"
- 자료 원본: scratchpad `sauceresearch_*.json` 4개 / 비교표 `_preview/_sauce_products.html`
- ⏳ **남은 것**: 가격은 2026-07 조사·추정값 → **쿠팡 파트너스 딥링크 만들 때 실가격 재검증 필요**. 피자소스(15줄)는 시판 브랜드 미조사라 기존 generic 유지.

## ✅ 진짜 로고 + 홈페이지 URL 교정 (2026-07-16)
- **문제1**: 기존 logo_url은 Gemini 추측 → 122개 중 6개만 로딩(116개 404). 핫링크 대부분 차단.
- **문제2(더 심각)**: **homepage_url도 35개가 틀림**(존재하지 않는 도메인). "로고 클릭→공식홈" 링크가 죽는 상태였음.
- **해결**:
  1. `scripts/fetch_logos.py` — 공식사이트에서 로고 직접 다운로드 → `_preview/logos/` 에 **자체 호스팅**(핫링크X, 안 깨짐). 우선순위 apple-touch-icon > icon > og:image > 구글 파비콘.
  2. `scripts/upgrade_logos.py` — 16~32px 파비콘을 큰 버전으로 업그레이드(17개. 예: 마왕족발 800px, 반올림 600px, 메가 630px)
  3. 홈페이지 35개 실접속 검증 후 교정: 빽다방 `paikscoffee`→`paikdabang.com`, 던킨→`dunkindonuts.co.kr`, 한솥→`hsd.co.kr`, 메가→`mega-mgccoffee.com`, 지코바 `gcoba`→`gcova`, 곱창고→`gobchanggo`, 퓨니코드 4건(구구족.com 등). 롯데리아는 인증서만료+WAF403 → `lotteeatz.com/brand/ria` 대체. 순수치킨·유로코피자는 도메인 소멸 → SNS.
  4. 렌더: `logo_html()` — 로고 있으면 `<img class=blogo>`, 없으면 텍스트 배지 자동 폴백. (loading=lazy는 제거 — 프리뷰에서 로드 안 되는 문제)
- **결과: 전체 122 중 로고 108 / 페이지 나오는 89개 중 80개 실로고**(나머지 9개 텍스트 배지: 지코바·신전떡볶이·응급실·아딸·가장맛있는족발·보승회관·이비가짬뽕·에머이·곱창이야기)
- ⚠️ **slug은 옛 도메인 기준 그대로**(빽다방 slug=`paikscoffee`, 던킨=`dunkinkorea`). URL이 `/cafe/paikscoffee/`가 됨 → **발행 전에 slug 재생성 권장**(지금이 적기, 색인 전이라 리다이렉트 불필요)

## ✅ 돼지 KAMIS 부위 버그 수정 (2026-07-16)
- **원인**: KAMIS 4304(국내산 돼지)는 삼겹 등 **주요부위 대표가** → 족발·등심에 쓰면 2~3배 과대 + 프랜차이즈는 보통 **수입산**
- `pork_trotter` 족발 생육(수입 냉동): 17,050 → **6,500원/kg** manual (근거: 수입 칠레산 냉동 장족 도매 4,650~4,980원)
  - 족발 원가율 82.4% → 25~35%로 정상화 (홈 화면 원가율 TOP에서 족발 빠짐)
- `pork_loin_tangsuyuk`·`pork_loin_donkasu` 등심(수입 냉동): 15,500~16,275 → **9,000원/kg** manual (근거: 미국산 냉동 등심 돈가스용 6,900·캐나다산 6,950원/kg 도매 × 소매마진) — 37개 메뉴 영향, 홍익돈까스 57.9%→38.7%
- KAMIS 코드는 NULL 처리해 collect_prices가 다시 덮어쓰지 않음. `pork_belly_import`(4402 수입삼겹)는 정상이라 유지.

## ✅ 재료 단가 전수 실시세 반영 (2026-07-16)
- **방법**: 웹서치 4그룹 병렬(수산9·소특수부위16·가공유제품19·KAMIS검증6) + **KAMIS 수산(600) 공공데이터 교차검증**(우리 키로 조회 가능, 스냅샷 `data/research/kamis_seafood_2026-07-15.json`)
- **적용 36건 / 보류 3건 / 유지(적정) 18건** — `scripts/apply_prices.py`, 검수표 `_preview/_price_audit.html`
- **영구 단가표**: `data/research/ingredient_prices.json` (전 항목 출처URL·근거상품·신뢰도 포함 → "그때그때" 개별 수정 가능)
- 주요 교정: 연어 45,000→32,000 · 새우살 25,000→14,000 · 광어 40,000→22,000 · 참치 35,000→25,000 · 도가니 20,000→9,000 · 전지 12,000→9,000 · 원두 30,000→18,000 · 버거번 700→400/개 · **계란 KAMIS 단위버그(개수단위를 kg로 오용, 2.5배 과대)→4,000/kg** · **김 단위오류(100장≈240g)→54,000/kg**(+'장'단위 2건 g환산) · 상향: 돈뼈 3,000→6,000 · 당면 4,000→8,000 · 생크림→11,000/L
- **부재료 27종**(1~3회 사용, NULL단가로 est_cost 재계산 차단하던 것) 추정 입력 → **2026-07-17 웹서치 전수검증 완료**: 적용 14(브라우니 12k→25k, 쿨라임 8k→15k, 소꼬리 18k→24k, 우거지→7k 등) / 유지 10 / low보류 3(롤시트·고추속·얼큰다시). 포기김치도 사용자 지적으로 4k→8k 교정+김치찌개류 4개 메뉴에 김치 재료 누락 발견·추가. 결과는 단가표 items_filler
- 결과: 원가율 이상치(90%↑/5%↓) **0건**. 새우버거 68→44.4% · 연어초밥 62→37~46% · 뼈해장국 76.6→44.2%
- ⏸ 보류: 염지닭 6,000(도매 마리당 기준이라 소매 컨셉과 상충—검토), 머리고기순대돈족(구성비 편차 low)
- **📌 단가 원칙(사용자 확정 2026-07-16): 왠만한 건 수입산·냉동벌크 기준.** 예외: 메뉴명에 '한우' 명시(한우곱창구이 등)는 한우가 유지, 닭고기(치킨 프랜차이즈 국내산 표준)·우유·계란은 국산.
  - 원칙 반영: 등뼈 6,000→**3,000**(수입 캐나다·칠레 도매 2,150~2,600, 사용자 지적) · 무뼈닭발 16,000→**12,500**(태국산) · 모둠햄 13,000→**9,000**(업소용 벌크) · 비엔나 9,000→**6,500**(벌크) · 수입삼겹 KAMIS냉장(15,370)→**12,000 manual**(냉동벌크)
- **단위 불일치 스캔·수정**: base_unit=kg인데 개/세트/장으로 계량된 5건 — 김 '장' 2건 g환산, 겉절이 1세트(5,000원 오계산→개당 500원), 우동사리 1개(4,000원→200g), 순대국밥 닭다리살 1세트(오매핑→치킨무). 시래기국 68→15.3% · 무공우동 67→18.0% 정상화

## ✅ 판매가 전수 웹서치 검증 (2026-07-16)
- 371개 전 메뉴를 8그룹 병렬 웹서치(공식 홈페이지 실측·인상 보도·배달앱 메뉴판) — **변경 178 / 일치 55 / 보류 138**
- 원인: Gemini 수집값이 2023~24 구가격. 2025~26 인상 러시(치킨 자율가격제·버거 2~5월 인상·스벅 2회 인상) 미반영
- 주요: 교촌 허니콤보 23,000→**26,000**(배달 이중가격) · 가마로 +9,000(메뉴개편) · 지코바 +2,500 · 반올림 고구마 +5,000 · 원할머니보쌈 +7,000~9,000 · 스벅 전종 +200~400 · 컴포즈 아아 1,500→1,800 · 역방향(과대): 버거킹(배달가 수집됨→매장가), 굽네, 파리바게뜨(2026.3 인하)
- **테이크아웃 컵**(사용자 지적): 컵+뚜껑+빨대 150원/개 재료 신설 → 카페 음료 30개 반영. 저가 아아는 컵이 원가의 29%
- 기록: `data/research/sell_prices.json`(출처 포함) · 검수표 `_preview/_sell_audit.html`
- **최종 상태: 평균 원가율 29.4% · 이상치 0건.** 허니콤보 42.3%·뿌링클 42.8%·빅맥 44.8%·국밥 29%·아아 25.5%
- ⏸ 보류 138건 = 미확인(지점편차·가격미공개)+low: 기존값 유지, _sell_audit.html에 사유 표시
- ✅ **단종/유령 메뉴 정리 완료 (2026-07-16)**: 371→**349개** (삭제 22 · 백업 `data/research/removed_menus.json` — 복원 가능)
  - 단종 삭제 11: BBQ 바삭갈릭·도미노 블랙앵거스·청년피자 에그머니나·한솥 제육/메가치킨제육·핵밥 소곱창/닭갈비덮밥·신전 한입고추·청년다방 넓적당면/다모아·메가 민트크림초코
  - 브랜드째 제외: **샐러드판다 7**(메뉴 전체 웹 확인불가, 고스트브랜드)·**미카도스시 4**(회전초밥 균일가 체제라 세트 실체 불일치) → 메뉴 있는 브랜드 89→87
  - 현행명 변경 7: 차돌떡볶이→불향차돌, 그린티→제주말차 프라푸치노, 녹차→말차빽스치노, 빽스라떼→카페라떼(3,200), **마왕족발 (소)→(중) 41,000 실체화**(+생육 1.3kg)·소금구이→통구이(소) 35,000

## ✅ 재료 '양' 상식범위 스캔 (2026-07-16)
- `scripts/scan_amounts.py` — 메뉴 유형별 룰(총중량·육류량·품목한도)로 371개 스캔 → 플래그 20건
- 판정: **대부분 룰 한계**(치킨 총중량에 튀김유 포함, 음료는 물·얼음 미계상, 초밥 32p 생쌀 250g은 정확). Gemini 양이 유튜버 원가분석 기반이라 대체로 건전
- **공표 중량 대조(버거 패티)**: 빅맥 90g=공식(45×2) 정확 ✓. 교정 5건 — 와퍼 3종 150→**113g**(공식 4oz), 롯데리아 불고기·데리 80/60→**50g**(소형 패티)
- 남은 양 검증 수단: 플래그 항목의 유튜버 원가분석 대조(menus.source에 출처 유튜버 기록됨) — 필요시 3단계 진행

## ✅ 일반 메뉴(dish) 축 신설 (2026-07-16, 사용자 아이디어)
- **브랜드 축과 별개로 "짜장면·김치찌개·탕수육" 요리 단위 페이지** — 검색량은 이쪽이 더 큼("짜장면 원가" > "홍콩반점 짜장면")
- 구조: dishes / dish_ingredients(표준 1인분 레시피) / dish_menus(브랜드 메뉴 매핑) 테이블 + `scripts/gen_dishes.py`
- **35개 dish + 228개 메뉴 매핑**. 페이지 구성: ①집에서 만들면 재료비(우리 단가로 계산+절약액) ②브랜드별 비교표 ③시판소스 복사→쿠팡(is_retail 자동 노출)
- 예: 짜장면 1인분 재료비 **1,844원** vs 프랜차이즈 평균 8,000원 → "6,156원 절약" 문구 자동 생성
- 산출: `_preview/dish_{slug}.html` 35개 + `dishes.html`(사전 목록), 홈에 배너 진입로
- ⏳ 표준 레시피 35개는 큐레이션 추정치(검수 대상). dish도 site-renderer 워커에 포함 필요
- dishes.html은 **검색창 + 배민식 카테고리 7개**(치킨/분식/중식·아시안/한식/일식·양식/샐러드/카페·디저트)
- **기준량(사용자 확정)**: 1인분 식사류는 **4인분·가족 기준**으로 표시(포장 단위 구매와 현실 정합) + 1인분당 환산 병기. 단위 메뉴(치킨 한마리·피자 한판·버거 1개·김밥 1줄·음료 1잔 등)는 그대로. 절약 문구도 "프랜차이즈 4그릇 vs 집 4인분"
- **포장단위 안내문**(사용자 지적): dish·메뉴 페이지에 "쓴 양만큼 환산한 금액, 첫 장보기는 더 듦, 여러 번 만들수록 저렴" 명시

## ✅ 시세 등락 표시 (2026-07-16, 사용자 아이디어)
- **live 재료 7종**(생닭·윙봉·닭다리살·채소·샐러드채소·쌀·우유)의 최신 vs 직전 시세일 등락을 ▲빨강/▼파랑/보합으로 표시
- 홈: "📈 오늘의 재료 시세" 위젯(7종 그리드) / 메뉴 페이지: 재료비 아래 등락% / dish 페이지: 합계 아래 등락%
- 등락 계산: live 재료 line_cost만 직전가로 치환해 재계산 (manual 재료는 고정)
- ⚠️ **"매일 업데이트"의 전제 = collect_prices.py 크론**(현재 price_history는 07-10·07-15 두 시점). 배포 시 Cloudflare cron으로 매일 실행해야 "전일 대비"가 됨. 라벨은 자동으로 "MM.DD 대비" 표기

## ✅ 매장 원가율 전환 (2026-07-17, 사용자 확정)
- **배경**: 사용자 지적 "피치세트 원가율이 이렇게 높으면 남을 수가 없다, 30%일 것". 조사 결과 **사용자 감각도 우리 숫자도 맞았고 기준이 달랐음** — 소매가 51.7% vs 도매환산 31%. 사이트 이름이 "얼마남아"(=얼마 남나)인데 헤드라인이 소매 기준이면 업계인 신뢰를 잃음 → **매장(도매) 원가율을 메인으로 전환**
- **도매가 확보**: `ingredients.wholesale_price/wholesale_basis` 신설. 우선순위 **KAMIS 실측 3 · 웹조사 39(미트박스·식봄 등 3그룹 병렬) · 재료군 계수 237**. 출처·근거상품 전량 `data/research/wholesale_prices.json`. 스크립트 `apply_wholesale.py`
  - 계수: 채소 0.52(KAMIS실측)·쌀 0.99·시판소스 0.60·농축산물 0.70·빵생지 0.75
  - **발견**: 우리 "소매가"가 이미 업소가 수준인 재료 다수(족발 0.91·치킨무 0.88·식용유 0.86·피자도우 0.95) → 도매 전환 효과가 재료마다 다름. 격차 큰 건 김치 0.43·떡볶이양념 0.46·양념치킨소스 0.48·튀김가루 0.53·한우부산물 0.56
- **계산**: `compute_line_costs.py`에 `menus.store_cost/store_ratio` 병행 계산(기존 단위환산·안전장치 재사용). **348/349 계산, 평균 19.8%, 이상치 0**
- **렌더**: 메뉴 요약카드 = 🏪매장 추정 원가(헤드라인)+원가율/차액, 그 아래 🏠집에서 만들면(소매) 병기. 재료 테이블 도매가 컬럼을 `wholesale_price` 기반으로 교체(기존 3종→전 재료). 홈·브랜드·카테고리·검색인덱스 모두 `store_ratio`. **dish 페이지는 소매 유지**("집에서 만들면" 컨셉)
- **결과**: 피치세트 51.7→**39.5%** · NBB 52.7→**32.2%** · 뿌링클 40.6→**30.6%** · 오징어튀김 42.9→**29.8%**. 매장 원가율 TOP도 36~42%로 업계 감각과 일치
- ⚠️ 면책: 매장 원가는 도매 공급가 기반 **추정**(본사 공급가·물류비·로스 미반영). 계수추정 237종은 재조사 여지
- **재료 양 교정도 동반**(사용자 지적): 식용유를 붓는 양→**흡수분(튀김물 12%)** 91개 메뉴 재계산(8,015→4,317ml) · NBB 패티 100→80g·치즈 40→20g(213g 전체 기준) · M피자 치즈 180→130g · 오징어 대왕오징어(수입) 12,000 · 신전 오징어튀김 120→90g

## ⚠️ 라이브 테스트로 발견 (수정 필요)
- 닭(9901): KAMIS가 0원 반환 → 축평원으로 대체해야(닭 정밀화) — 축평원 작동 중, 소매 검증 결과 적정
- ~~소고기 다짐육/우삼겹~~ ✅ 실시세 반영됨(16,000/13,000)
- ~~계란(9903): 10구 단위 → kg 환산 필요~~ ✅ manual 4,000/kg 전환 완료

## ⬜ 공공데이터 활용신청 (사용자가 직접 눌러야 함) — 공정위 매장수·매출

**왜 필요:** 브랜드 페이지에 "전국 N개 매장 · 평균매출 X억" 표시(정확한 공식데이터). 신뢰도↑
**인증키:** 기존 키 `01d41ee3...` 그대로 사용(새 발급 불필요). 활용신청만 하면 됨(자동승인).

### 하는 방법 (각 API마다 반복, 한 번씩)
1. https://www.data.go.kr 로그인 (참가격 받은 계정, dndmotor1@gmail.com)
2. 아래 링크 접속 → 페이지 **오른쪽 파란 "활용신청" 버튼** 클릭
3. "활용목적" 칸에 아무거나(예: 웹서비스 개발) 적고 → **신청** → 자동승인(바로 됨)
4. 신청한 페이지에서 **"미리보기" 탭 → 나오는 요청 URL 전체를 복사** → Claude에게 전달
   (그 URL에 정확한 엔드포인트가 들어있어서 그게 있어야 연결 가능)

### ✅ 공정위 API 승인·작동 확인 완료 (2026-07-16) — 바로 연결 가능
- **엔드포인트**: `http://apis.data.go.kr/1130000/FftcBrandFrcsStatsService/getBrandFrcsStats`
- **파라미터**: `serviceKey`(기존 키)·`yr`(년도, 필수 예:2023)·`pageNo`·`numOfRows`
- **우리 키로 승인 확인됨**(resultCode 00, 2023년 11,167개 브랜드)
- **응답 필드(12개)**: brandNm(브랜드명), corpNm(법인명), frcsCnt(가맹점수),
  avrgSlsAmt(평균매출금액), arUnitAvrgSlsAmt(면적당평균매출),
  newFrcsRgsCnt(신규), ctrtEndCnt(종료), ctrtCncltnCnt(해지), indutyLclasNm/MlsfcNm(업종)
- 710(평균매출 별도) 불필요 — 241 하나에 매출 다 들어있음

### ✅ 공정위 매장수·매출 연결 완료 (2026-07-16)
1. `scripts/fetch_fftc_raw.py` — `FFTC_YR=2023|2022|2021` 각 전체 fetch → `data/fftc_{yr}.json` 캐시 (11,167/10,185/10,242건)
2. `scripts/collect_fftc.py` — brands 122개 매칭 후 컬럼 주입 + **연도 폴백**
   - 매칭: 정규화 exact + **수기 별칭 33건**(MANUAL dict, 법인명까지 대조). 오탐 차단(수기 지정 시 그 이름만 신뢰).
   - **연도 폴백 2023→2022→2021**: 최신에 없거나 매장 0건이면 이전 연도 실데이터 채택, 브랜드별 실제 연도를 `fftc_yr`에 저장(페이지에 "(YYYY 공정위)" 정직 표기)
   - **결과: 유효 118 / 미매칭 4** (총 122). 연도분포 2023:99 · 2022:17 · 2021:2
   - 2022 복구 예: BBQ(비비큐 2002점·4.4억), 롯데리아(1211·7.1억), 투썸(1330·5.1억), 메가MGC(1593·3.3억), 멕시카나·보드람·큰맘·땅스·와플대학·호식이·코코이찌방야·59쌀피자(오구쌀피자)·가마로강정·이화수·가장맛있는족발
   - **끝까지 미매칭 4개**(NULL 미표시): 쉐이크쉑·스타벅스(직영이라 가맹통계 없음), 샐러드판다(미등록), 동대문엽기떡볶이(원브랜드 영남에프앤씨 미등록 — 유사명 '불닭발땡초…'는 별개라 매칭 안 함)
3. brands 컬럼 추가: fftc_yr / fftc_brand_nm / fftc_corp_nm / frcs_cnt / **avrg_sls_amt(천원)** / ar_unit_avrg_sls. schema.sql에도 반영.
4. `gen_preview_html.py` 렌더 완료:
   - 브랜드 페이지: 카드형 "🏪 전국 N개 가맹점 / 📊 가맹점당 평균 연매출 X억원 (YYYY 공정위)"
   - 메뉴 페이지: 한 줄 pill (브랜드명 아래)
   - frcs_cnt>0 일 때만 표시(미매칭 자동 숨김). avrgSlsAmt 천원→억/만원 환산, 매출 0이면 매장수만 표시.

### 메뉴 가격 데이터 관련 (확인 결과)
- 공정위 공공데이터엔 **개별 메뉴 판매가 없음**(배달앱/홈페이지에만). 우리 배달앱 기준값이 최선.
- 정보공개서엔 "필수품목·차액가맹금(본사 재료 마진)" 있으나 API는 요약치만, 상세는 PDF.

## ⬜ 남은 작업 (다음 세션)

### 🎯 이번주 목표(사용자 확정 2026-07-17): workers.dev로 **도메인 없이 우선 배포**
0. **배너 숨김 상태** — 애드센스 자리+쿠팡 iframe display:none, 슬롯(ad-home/ad-menu-top/cp-menu) 유지. `SHOW_ADSENSE`/`SHOW_COUPANG_BANNER` 플래그로 재노출. 복사버튼은 살아있음
1. ~~slug 재생성~~ ✅ (2026-07-17) 17건 교체: paikdabang·dunkindonuts·megamgccoffee·sangmusushi·gcova·gobchanggo·keunmam·100chae 등. 법인명/플랫폼/약어 도메인(shinsegaefood·theborn·hsd·lotteeatz)·퓨니코드는 기존 slug 유지. 충돌 0
2. ~~레시피~~ ✅ (2026-07-17) **349/349 생성 + ~하세요체 통일 완료** — 검증된 재료·양·시판소스 실명 기반 3~5단계. 말투 변환은 3청크 병렬 후 자동검증(구조·수치보존·잔여한다체 모두 0) 통과. 백업 `data/research/recipes.json`
3. **site-renderer 워커 (templates.js)** ← 다음 작업. academy 복제 + dish 축 + 시세위젯 + logos(R2/assets) + D1 시딩 + wrangler.toml. **배포 시 사용자 wrangler 로그인 필요**
4. 시세 크론(collect_prices 매일) — 워커와 함께

### 여유될 때
5. 시판소스 가격 재검증(파트너스 딥링크 시점) / 로고 9개 수동 / 저신뢰 재료 4건 / 피자소스 시판 매핑 / 재료 중복키 정리 / dish 표준레시피 검수 / 레시피 말투 통일

## 📁 산출물 위치 (2026-07-16 정리)
- **DB**: `data/eolmanama.db` (브랜드122·메뉴371·재료255·로고108)
- **로고**: `_preview/logos/` 108개 (자체 호스팅. 배포 시 R2 이전 필요)
- **자료조사 원본**: `data/research/`
  - `corrections_*.json` (12) — 카테고리별 재료 감사 결과(웹검증 포함)
  - `sauceresearch_*.json` (4) — 시판소스·밀키트 조사(제품·용량·가격·쿠팡여부)
  - `fixed_homepages.json` / `broken_homepages.json` — 홈페이지 35개 교정 근거
- **공정위 캐시**: `data/fftc_{2021,2022,2023}.json`
- **검수 페이지**: `_preview/_menu_audit.html`(재료371) · `_sauce_review.html`(소스250) · `_sauce_products.html`(시판소스30종)
- **프리뷰 실행**: `cd _preview && python -m http.server 8790` → http://127.0.0.1:8790 (8899는 방구석약국 소유)

## ⚠️ 검증 상태 (다음 세션에서 반드시 인지)
- ✅ **검증됨**: 홈페이지 122개 전부 HTTP 200 실접속 / 공정위 매칭 전수대조 / 재료 오매핑(13카테고리 병렬감사+웹) → 감사표 🔴0 🟠0 / 원가 재계산
- ⚠️ **미검증**: **로고 108개 중 실제 눈으로 확인한 건 2개뿐**(나머지 106개가 진짜 그 브랜드 로고인지 미확인 — 최우선 검수 대상) / 소스배합 250개(제 지식 기반, 제3자 미검증) / 시판소스 가격 23종(쿠팡 봇차단 → 추정값) / 재료 양·그램수 전부 추정 / 저신뢰 4건

## 참고
- 브랜드 로고 클릭 → 공식 홈페이지 (트래픽 선순환). 하단 면책문구 필요.
  - ⚠️ **현재 메뉴 페이지만 로고가 링크**. 브랜드 페이지는 로고 클릭 안 됨(아래 "공식:" 텍스트만 링크) → 통일 필요
- 소매가 기준(도매가 토글은 나중/B2B). 판매가 vs 재료비 대비가 핵심.

## 2026-07-17 — 자체 감사 루프 (Pass 1~6)

사용자 지시: "코딩적이나 다른 쪽 문제 있나 없나 계속 루프를 돌려라."
찾은 것과 근거를 남긴다. 매번 다시 돌릴 수 있게 **`scripts/healthcheck.py`(26종 자동점검)** 로 고정했다.

### 발견·수정한 실제 버그
| # | 버그 | 원인 | 영향 |
|---|---|---|---|
| 1 | 우유 42개 메뉴 원가 미갱신 | `to_base()`가 kg↔ml 교차환산 불가 → 180ml×2,938원/kg=528,840원이 안전장치에 걸려 **조용히 스킵** | est_cost 재계산 차단 (306→349) |
| 2 | 349개 메뉴 브랜드 링크 깨짐 | `brand_{slug}.html`로 링크, 파일은 `brand_{id}.html` | 전 메뉴 breadcrumb |
| 3 | 레시피 467 없는 제품 호출 | 재료는 "시저와사비"인데 레시피는 "오뚜기 시저 드레싱" | 1건 |
| 4 | **표 도매합계 ≠ 헤드라인 매장원가** | 소스 행이 도매가를 `-`로 하드코딩하고 합계엔 **소매가**를 더함 | 허니콤보 10,129 vs 9,729 (400원) |
| 5 | 빽스치노 등 4건 2원 오차 | 렌더러가 도매를 재계산(`소매×비율`) → 반올림 2회 | 4건 |
| 6 | 프리뷰 포트 충돌 | 문서가 8899(방구석약국 소유) 지정, 8790은 얼마남아-b2b\web 점유 | → **8792**로 확정 |

### 구조 개선
- **단일 출처**: `menu_ingredients.wholesale_cost` 컬럼 신설. 도매 라인가격은 `compute_line_costs.py`가 계산·저장하고 렌더러는 읽기만. 4·5번의 재발 원인 자체를 제거 → 349/349 표 합계 = 헤드라인 정확히 일치.
- **스킵 리포트**: `compute_line_costs.py`가 단위불일치·이상치 스킵을 끝에 출력. 1번이 반년 숨어 있던 이유가 조용한 스킵이었다.
- **정적검사**: healthcheck가 AST로 "커서 직접순회 + 본문 재쿼리" 패턴을 검출(prices.html 47종→2종 버그의 재발 방지).
- **멱등성 확인**: 생성기 4개 + compute를 2회 연속 실행해 DB 해시·HTML 해시 동일 확인. 출력에 시각/난수 없음.

### 내 검사기가 틀렸던 것 (오탐 — 사실 아님)
"레시피 말투 이탈 257건"은 전부 정상이었다. 정규식이 `주세요.`(마침표)와 `내세요/살아요/않아요` 활용형을 못 잡은 탓. 존댓말 **허용목록**으로 검사하면 오탐만 나므로, **한다체 검출**(금지목록)로 뒤집었다 → 0건.
같은 이유로 이전 감사의 "제품 언급 174 / 튀김 2 / g불일치 10 / 원산지 13"도 오탐이었다. **검사기가 뭘 놓치는지부터 의심할 것.**

### 남은 미검증 (코드가 아니라 데이터)
healthcheck는 **계산·구조**만 본다. 값 자체가 맞는지는 못 본다. 최대 리스크는 여전히 **로고 108개 중 106개 미확인**과 **도매가 계수추정 237종**.

## 2026-07-17 — 재료 시세 페이지 재설계 (검색·선택형)

사용자 지적: "이 밑으로 너무 내려가는데. 디자인 쪽으로 도움이 안 된다. 선택하게 만들어서 선택 창에서 골라 바로 보이게. 쭉 나열하는 건 아닌 것 같다."
목업 A/B 두 안을 실제 데이터로 만들어 보여주고 **A안(검색·선택형) 확정**.

### 왜 A안인가 (360px 실측)
| | 스크롤 | 담는 재료 |
|---|---|---|
| 기존 (세로 나열) | **4.3화면** | 47종만 |
| **A안 검색·선택형** ✅ | **1.5화면** | **257종 전부** |
| B안 탭+그리드 | 첫 화면 1.1화면 — **가공 탭 10.5화면** | 257종 |

B안은 나열을 탭 뒤로 감췄을 뿐 없애지 못했다(가공 179종). A안은 무엇을 고르든 화면 길이가 일정.

### 구조
- **오늘 요약**(오름 4 / 내림 5 / 보합 3) → 검색창 → 분류 칩(전체·매일시세·축산·농산·수산·가공·시판) → **선택한 재료 1종 상세 카드** → 오늘 움직인 재료(가로 스크롤)
- 기본값 = **오늘 가장 많이 움직인 재료**. "골라야만 보인다"는 빈 화면 문제를 막는다.
- 상세 카드: 현재가 / 등락 / 쓰는 메뉴 수 / **매장 도매가(추정)** / 이 재료가 원가에서 큰 메뉴 3개 링크
- 엔터 = 첫 검색결과 선택. 칩을 바꾸면 그 분류의 대표 재료를 바로 표시.

### ⚠️ 되돌리면 안 되는 것 — 정적 링크
화면은 JS가 그리지만, **`<details>` 안에 257종 전체 목록을 정적 HTML로 유지**한다(접힘이 기본이라 화면 길이엔 영향 없음).
- 링크까지 JS로 만들면 **① 검색엔진이 못 탄다(pSEO 사이트의 핵심 자산) ② healthcheck의 깨진링크 검사가 무력화**된다.
- 실제로 재설계 직후 링크 검사가 "참조 메뉴 0개"로 나왔다 — 깨져도 못 잡는 상태였다. `<details>` 추가로 정적 링크 259개 복구.
- 47종 → 257종: 기존 페이지는 길기만 한 게 아니라 **210종이 아예 빠져 있었다**.

## 2026-07-17 — 채소 감사 → 보쌈 고기 문제 발견 (미해결, 사용자 조치 필요)

사용자 질문 "신선채소는 다 따로따로 분류해야 돼?"에서 시작. 파고들다 더 큰 걸 찾았다.

### 1. 신선 채소의 정체 규명 (완료)
`vegetables_fresh` = live_composite, KAMIS 코드 **245·246·212**인데 **그 코드가 뭔지 어디에도 없었다.**
홈 첫 화면에 "신선 채소 2,442원 ▲5.7%"를 띄우면서 정작 우리가 그게 뭔지 설명 못 하는 상태였고, 184개 메뉴 원가가 여기 걸려 있었다.

→ 우리가 매일 호출하는 **동일 엔드포인트**(`dailyPriceByCategoryList`, cat=200) 응답에서 확인(추측·신규 엔드포인트 아님):
**245=양파(40%) · 246=파(30%) · 212=양배추(30%)**
- 채소류 29품목 코드표 영구 저장 → `data/research/kamis_item_codes.json`
- `gen_ingredients_seed.py`에 주석 고정, **시세 페이지 화면에도 근거 표기**(`BASIS` dict → 📐 배지)

### 2. 지수가 맞는 곳 / 틀린 곳
| | 판단 |
|---|---|
| 짜장면·중식볶음 | ✅ 실제로 양파·양배추·대파를 쓴다 — **그대로 두는 게 맞다** |
| 마라탕 17건 | ❌ 실제는 청경채·배추·숙주·팽이·목이·느타리 (탕화쿵푸 공식 메뉴판 채소 카테고리) |
| 보쌈/족발 13건 | ❌ 실제는 보쌈김치·무김치·쌈무·절임배추 = **대부분 절임 가공품** |
| 공심채 1건 | ❌ 에머이 '공신채'='공심채(모닝글로리)' 확정. **KAMIS 미수록 수입 특수채소** |

사용량 분포(184건): 150g↑ 38건(21,626원) / 50~150g 98건 / 50g 미만 48건(4,059원).
**전체 재료비의 3.6%뿐** → 146건은 쪼개는 게 손해(원가 영향 1% 미만인데 미검증 추정치만 늘어남).

### 3. ❌ 그램 분리는 하지 않았다 (의도적)
조사팀 3개 전부 동일 결론: **품목별 g은 어떤 공개 출처에도 없다.** 프랜차이즈는 채소 중량 비공개.
- 마라탕 3사 = 셀프바 + g당 과금 → **"표준 250g 구성"이 원리적으로 존재하지 않음** (필라이즈는 300g 표기로 우리 250g 가정과 충돌)
- 여기서 g을 채우면 사용자가 지적했던 "확인도 안 하고 그대로 믿고 넣은" 사고의 재발
- → `data/research/vegetables_audit.json`

### 4. 🔴 진짜 문제: 보쌈 고기 (채소 1,221원 < 고기 7,200원)
채소를 파다 발견. **둘 다 근거가 없다.**
- **중량**: 원할머니 공식 생육 500g vs 우리 600g = 20% 과대 의심. 돈육÷생육이 소·중·대 **전부 70%**로 일치(삶은 수율). 단 소(小)가 스니펫 1회 근거(medium)라 **미적용** — 여기서 고치면 또 미검증 수치를 넣는 셈
- **부위**: '수입 삼겹살' 가정에 **아무 근거 없음**. 보쌈 통상 부위는 앞다리살·목살. **17개 메뉴·재료비 5.4%**가 걸림
- **웹서치로는 원리적으로 못 푼다** — 음식점 원산지·부위는 **매장 내 게시 의무**이지 웹 공개 의무가 아님. 원할머니 공식 사이트 직접 확인 결과 중량·부위·원산지 표기 전무(설명 문구뿐)
- → `data/research/bossam_meat_audit.json`

### 5. 열쇠를 찾았으나 Claude는 못 연다 — 공정위 정보공개서
`franchise.ftc.go.kr` → 정보공개서 열람. 가맹본부가 **법적으로 제출**하는 문서라 필수품목·원재료가 들어 있다.
- **원앤원(주) / 영업표지 "원할머니" / 등록번호 20080500019** 확인. ⚠️ **영업표지는 브랜드 통칭과 다름** — "원할머니보쌈" 0건, **"원할머니"** 로 검색해야 나옴
- **상세 열람은 로그인 필수** → 계정 생성·로그인은 Claude 금지 작업. **사용자가 PDF를 받아 폴더에 주면 읽어서 반영**
- 이걸로 풀리는 것: 보쌈 부위·중량 + **도매가 계수추정 237종의 근거**
- → `data/research/ftc_disclosure_lead.json`

### 교훈
- **"✅ 전 항목 통과"는 값이 맞다는 뜻이 아니다.** healthcheck는 계산·구조만 본다. 이번 건은 전 항목 통과 상태에서 나왔다.
- 요청을 잘못 내면 답도 틀린다: 가격 조사팀에 "소매가"를 시켰는데 쿠팡·컬리·이마트가 봇 차단(403)이라 **식봄(B2B 도매)** 값을 가져왔다. 그대로 `manual_price`(소매)에 넣었으면 원가가 통째로 낮아졌을 것. → `wholesale_price` 후보로만 쓸 것
- 상추는 2026년 7월 **평시의 8~11배 폭등 구간**. 엽채류를 연간 고정단가로 박으면 왜곡된다.

## 2026-07-17 — 오매핑 전수검사 + 교정 10건

"수정사항 다 확인된거야?" 질문에서 시작. 하나씩 우연히 발견하던 걸 **기계가 잡게** 만들었다.

### 검출기: "메뉴명이 말하는 재료가 없음"
`healthcheck.py`에 상시 검사 추가. 메뉴 이름에 부위·재료가 명시됐는데 그 재료가 안 들어간 경우를 잡는다.
소대창→돼지등심 사고와 같은 계열. **12건 검출 → 오탐 2건 제외(떡갈비의 '떡' 등, `NAME_OK`에 사유와 함께 등록).**

### 교정 (근거가 명확한 것만)
| 메뉴 | 고침 | 근거 | 원가율 |
|---|---|---|---|
| 순남시래기 3대시래기국 | 신선채소→**삶은 시래기** | 브랜드가 순남시래기, 레시피도 '삶은 시래기 120g' 명시. **재료표만 딴소리** | 11.6→13.0% |
| 바르다김선생 크림치즈호두김밥 | 우유→**크림치즈** | 레시피 '크림치즈를 부드럽게 풀어' | 11.2→16.5% |
| 홍대개미 닭목살덮밥 | 닭다리살→**닭목살** | 별도 부위로 정상 유통(태국산 냉동 2,917/kg) | 11.8→7.9% |
| 고봉민 새우김밥 | 해물믹스→**새우살** | 새우살 재료가 이미 있는데 안 씀 | 17.5→19.6% |
| 차돌 3건(떡볶이·쌀국수·육개장) | 우삼겹→**차돌박이** | 차돌박이(양지머리)≠우삼겹(업진살). 17,580 vs 13,000 | 각 +1.4~1.7%p |
| **맥도날드 1955 버거** | 삼겹살→**베이컨** | 🔑 **공식 원산지표**: '베이컨_돼지고기: 외국산(아일랜드/스페인/캐나다)' | 25.6→24.6% |
| **도미노 포테이토 피자** | 삼겹살→**베이컨** | 🔑 **공식 원산지표**: '포테이토 — 베이컨(돼지고기:외국산)' | 17.7→16.9% |
| 핵밥 매운항정살덮밥 | 삼겹살→**항정살** | 메뉴명이 항정살. 삼겹(복부)≠항정(목덜미). 16,000 vs 12,000 | 17.8→19.9% |

삼겹살 가정: **17개 → 14개** (재료비 5.4% → 5.1%)

### 🔑 브랜드 공식 원산지표가 열쇠였다
맥도날드·도미노는 **공식 사이트에 원산지표를 공개**한다 → 부위를 확정할 수 있다.
반면 원할머니·놀부·가장맛있는족발은 **미공개**(매장 내 게시 의무일 뿐 웹 공개 의무 아님) → 보쌈 6건은 여전히 미해결.
**다른 브랜드도 공식 원산지 페이지가 있는지부터 확인할 것.** 지금까지 이 출처를 안 썼다.

### 새 단가 (전부 B2B 도매 실측, confidence high)
삶은시래기 3,550 · 크림치즈 13,350 · 차돌박이 17,580 · 닭목살 2,917 · 항정살 16,000 (원/kg)
→ `missing_ingredient_prices.json`, `pork_mismap_audit.json`

### ❌ 근거 없어 남긴 것 (지어내지 않음)
- **빽보이 울트라빽보이 L** — ⚠️ **더본코리아 공식 라인업 13종에 없음(단종 의심)**. 삼겹살 80g 근거 전무. 배달몰 표기는 '매콤불고기: 돼지고기(국내산)' = 수입도 삼겹살도 아님. **메뉴 유지 여부부터 결정 필요**
- **핵밥 고기듬뿍덮밥** — 항정살덮밥과 별개 메뉴(10,900 vs 11,900)인데 같은 부위로 묶여 있었음. 실제 부위 근거 없음
- 감자핫도그(레시피에 감자가 아예 없음) · 고구마통모짜 · 버섯소불고기 — 그램 근거 없음

### 내가 저지를 뻔한 것
1차 교정 초안에서 **새우 30g→60g, 차돌 80g→60g으로 양을 무심코 바꿨다.** DRY 실행에서 잡음.
**재료가 틀렸다는 근거와 양이 틀렸다는 근거는 별개다.** 교정 스크립트는 품목만 바꾸고 양은 보존하도록 고정.

### 로고 검수 시트 (`_logo_audit.html`)
로고 108개 중 **메뉴 0개 브랜드 30개는 페이지가 아예 생성되지 않아 사이트에 미노출** → 검수 대상 **78개**로 축소, 나머지는 접어둠.
클릭→결과복사 방식. localStorage에 저장돼 새로고침해도 유지. (브랜드 122개 중 35개가 메뉴 0 — 사이트는 "브랜드 87곳"으로 정확히 표기 중이라 버그 아님)

## 2026-07-17 — 로고 육안 검수 완료 (사용자)

`_logo_audit.html`로 **노출 중인 78개 전수 확인 → 17개(22%) 오류**. 자동 수집 로고를 검증 없이 두면 안 된다는 게 수치로 확인됐다.
탈락: 노브랜드버거·롯데리아·던킨·뚜레쥬르·감탄떡볶이·동대문엽기떡볶이·명랑핫도그·청담동마녀김밥·구구족·원할머니보쌈·홍콩반점0410·노랑통닭·처갓집양념치킨·설빙·도미노피자·빽보이피자·백채김치찌개

- **조치**: `logo_url=NULL` → 텍스트 배지 폴백(이미 14개가 그렇게 정상 노출 중이던 방식). **로고 91 · 배지 31**
- **왜 즉시 내렸나**: 남의 브랜드 로고를 다는 건 상표 리스크. 노출이 늘수록 위험도 커진다.
- 🔴 **`brands.logo_rejected=1` 가드 (지우지 말 것)**
  `fetch_logos.py`는 `WHERE logo_url IS NULL`로 대상을 뽑는다. 그냥 NULL만 하면 **다음 실행에 똑같이 틀린 로고를 다시 가져온다.**
  `fetch_logos.py`·`upgrade_logos.py` 양쪽에 `AND COALESCE(logo_rejected,0)=0` 가드를 넣었다. 이 가드가 빠지면 재발한다.
- → `data/research/logo_audit.json` (탈락 URL까지 기록 — 같은 걸 또 받지 않도록)

**교훈**: "미검증 106개"라고 백로그에만 적어두고 몇 세션을 넘겼다. 실제로 검수하니 22%가 틀렸다.
Claude가 못 하는 검증(육안·브랜드 식별)은 **사용자가 3분에 끝낼 수 있게 화면을 만들어 넘기는 게** 가장 빠르다.

## 2026-07-17 — 로고 재수집 2차 (사용자: "찾기 힘든 건 이름만 써")

### 1차가 22% 틀린 진짜 원인
`fetch_logos.py`가 폴백으로 **`og:image`를 썼다.** og:image는 로고가 아니라 **홍보 배너·음식 사진**이다.
명랑핫도그가 음식 사진, 롯데리아가 버거 사진이었던 게 전부 이것 때문. → `refetch_logos.py`는 **og:image를 아예 쓰지 않는다.**

### 2차 전략 (`refetch_logos.py`)
헤더/네비의 **진짜 로고 `<img>`** 를 찾는다: src/class/alt/id에 `logo|symbol|bi_|ci_|brand` 힌트가 있는 img (1순위)
→ apple-touch-icon (2순위) → favicon (3순위). `sprite|banner|bg_|event|popup|sns|btn` 등은 제외.
**31개 중 19개 수집 · 12개는 후보 없음 → 이름 배지 확정**

### Claude가 먼저 눈으로 걸러냄 (19 → 11)
받은 걸 자동으로 믿지 않고 Chrome으로 확대해 확인. **명백히 로고가 아닌 8개 제거** → 사용자 부담 19→11개.
| 탈락 | 실제로 뭐였나 |
|---|---|
| 구구족 | 식칼(cleaver) 사진 |
| 롯데리아 | 버거·감자튀김·콜라 음식 사진 |
| 뚜레쥬르 / 백채김치찌개 | 빈 이미지 (화면에 아무것도 안 뜸) |
| 원할머니보쌈 | favicon.ico 16px — 식별 불가 |
| 청담동마녀김밥 | 정체불명 빨간 도형 |
| **빽보이피자 / 홍콩반점0410** | 더본코리아 공통 'BEST' 가게 일러스트. **두 브랜드가 바이트 단위로 동일 파일** |

> 자동수집 결과의 **md5 중복 검사**가 유효했다. 서로 다른 브랜드가 같은 파일 = 확정 오류.

### 잡은 버그: 검수 시트가 새 로고에 옛 판정을 되살림
localStorage가 `brand_id`만으로 '틀림'을 복원해서, **로고를 새로 받았는데도 예전 탈락 표시가 그대로 떴다.**
사용자가 보면 새 로고를 이미 거부한 걸로 오해한다. → 카드에 `data-ver`(현재 로고 파일명)를 심고
**id + ver가 모두 같을 때만 복원**하도록 수정(`logoBad` → `logoBad2`). 로고가 바뀌면 판정이 자동 무효.

### 현재
**로고 102 · 이름 배지 20 · 사용자 확인 대기 11**
2차까지 실패한 8개는 `logo_rejected=1`로 **이름 배지 확정**(사용자 지시: "그 다음에도 힘들면 이름만").
→ `data/research/logo_refetch.json`

### 로고 최종 확정 (2026-07-17)
사용자가 2차 11개를 전부 확인 → **"전부 맞음"**. 로고 작업 **완결**.

```
로고 102 · 이름 배지 20 · 재수집 차단 20
fetch_logos.py 다음 대상: 0개
```

🔴 **이름 배지 20개는 전부 `logo_rejected=1`로 잠갔다.** 사용자 지시("제대로 한번 더 찾아보고 그 다음에도 힘들면 이름만")대로
2차까지 시도하고 실패한 것이라 **자동수집을 다시 열면 안 된다** — 열면 식칼 사진·버거 사진·더본 공통 아이콘이 되돌아온다.
정확한 로고가 필요하면 **수동으로** 넣을 것.

**이번 로고 건의 교훈 3줄**
1. 자동수집 결과를 검증 없이 두면 **22%가 틀린다**. "백로그에 적어두고 넘어가기"를 몇 세션 반복했던 항목이었다.
2. **md5 중복 검사**가 값싸고 강력했다 — 빽보이/홍콩반점이 바이트까지 같은 파일인 걸 즉시 잡았다.
3. Claude가 못 하는 검증(육안·브랜드 식별)은 **화면을 만들어 사용자에게 넘기는 게** 가장 빠르다. 단 넘기기 전에
   **내가 먼저 명백한 것들을 걸러** 사용자 부담을 줄인다(19→11).

## 2026-07-17 — 심층 발굴 1라운드 ("안 나올 때까지 파라")

### 면 오매핑 6건 — '중식 생면'이 만능 면이었다
`noodles_fresh`(2,500/kg)를 쓰는 14개 메뉴 중 **6개가 중식이 아니었다.**
- **메밀면 3건**(포케올데이 들기름메밀면샐러드·메밀면포케, 샐러디 우삼겹메밀면샐러디) → ★ **메밀면(소바) 재료가 이미 DB에 있었는데 안 쓰고 있었다**
- **칼국수 2건**(바르다김선생 안동국시, 이화수 육개장칼국수) → `칼국수면(생면) 3,200` 신규 (식봄 9kg 28,500 = 3,167/kg)
- **파스타 1건**(홍익돈까스 까르보나라) → `스파게티면(건면) 4,500` 신규 (바릴라 1kg 4,600 / 디벨라 5kg = 4,324)

원가율: 까르보나라 6.7→8.6% · 들기름메밀면 4.4→6.3% · 안동국시 11.5→12.8% 등

### 중복·죽은 행 정리 (재료 284 → 264종)
- **대왕오징어가 5개 키로 쪼개져** 있었음 → 1개 병합(단가 동일이라 원가 무변화)
- **해물믹스**: 조사값 9,000(19메뉴) vs 추정 13,000(1메뉴) = **44% 차이** → 조사값으로 병합
- **찌개용두부**: 3,000(dish 2곳) vs 5,000(메뉴 1곳) → **같은 두부가 페이지마다 값이 달랐다** → 5,000 통일
- 죽은 행 16개 삭제 · 중복 이름 2건 → 0건

### 🔴 내가 낸 사고 — dish 페이지 35개가 죽었다
"안 쓰는 재료"를 지웠더니 `gen_dishes.py`가 **TypeError로 통째로 죽었다.**
**원인: dish 표준레시피는 DB가 아니라 `gen_dishes.py`의 하드코딩 `DISHES` 리스트에 있다.**
- 1차 실수: `menu_ingredients`만 보고 dead 판정 → **dish_ingredients에서 쓰는 6개**가 걸려 있었다(삭제 직전에 발견)
- 2차 실수: DB 양쪽을 다 확인했는데도 **스크립트 하드코딩**은 못 봤다 → `tofu_fresh` 삭제 → 요리사전 전멸
- **재발방지**: healthcheck에 3종 추가
  ① 고아 재료키(menu) ② **고아 재료키(dish)** ③ **gen_dishes.py DISHES가 부르는 없는 재료키**

> **교훈: "이 데이터를 누가 쓰는가"를 DB 안에서만 찾으면 안 된다.** 스크립트에 하드코딩된 참조가 있다.

### 이상 없음으로 확인된 것 (판 결과 깨끗)
| 각도 | 결과 |
|---|---|
| 같은 재료의 메뉴별 단가 불일치 | 0건 |
| 도매/소매 비율 극단 | 2건뿐 (연어·양념소스 = 조사값이라 정상) |
| 판매가 미반영 | verified≠DB 52건이 **전부 confidence=low → 의도적 보류**. high/medium 누락 **0건** |
| 카테고리별 원가율 분포 | 샐러드 15.2% ~ 치킨 29.2%. 이상 없음 |
| 가공품 단가 상식 밖 | 김 54,000/kg 1건뿐(김은 원래 가볍고 비쌈 — 정상) |

## 2026-07-17 — 심층 발굴 2~4라운드

### 🔴 4라운드 최대 발견: pSEO 사이트인데 SEO가 통째로 비어 있었다
사이트의 자산이 검색 노출인데, 노출을 만드는 장치가 하나도 없었다.

| 항목 | 발견 당시 | 지금 |
|---|---|---|
| **meta description** | **489개 중 488개 없음** (검색결과 스니펫이 안 나옴) | **488/488** |
| **sitemap.xml** | **없음** (구글이 489개 페이지를 알 방법이 없다) | 489 URL |
| **robots.txt** | 없음 | 생성(검수용 `/_` 차단 + sitemap 링크) |
| canonical | 0/489 | 488/488 |
| og:title/description | 0/489 | 488/488 |
| **중복 title** | **21종** | **0** |
| 링크 안 되는 고아 페이지 | 1개 | 0 |

- **중복 title 원인**: 메뉴 title에 브랜드명이 없어 브랜드 페이지와 겹쳤다(`홍익돈까스`가 브랜드명이자 메뉴명). 서로 다른 브랜드의 `볶음밥`·`분짜`·`순대국밥`끼리도 겹쳤다. → 메뉴 title을 **`{브랜드} {메뉴}`** 로 변경
- 🔴 **`sample_menu.html` 제거** — `menu_6.html`을 **바이트 단위로 복사**한 고아 파일이 발행되고 있었다. 개발 초기 '대표 메뉴 보존'용 잔재인데, 검색엔진엔 **중복 콘텐츠**로 잡혀 순위에 해가 된다.
- **description은 데이터로 생성** — 메뉴: `{브랜드} {메뉴} 판매가 X원, 매장 추정 원가 Y원(원가율 Z%)…` 식으로 페이지마다 다르게.
- ⚠️ **배포 도메인 확정 시 `SITE` 상수를 4곳에서 함께 바꿀 것**: `gen_sitemap.py` · `gen_preview_html.py` · `gen_dishes.py` · `gen_prices_page.py`

### 2라운드: 한 메뉴에 같은 재료가 두 줄 (15건)
재료비 표에 **"우유 200ml 588원" / "우유 40ml 118원"이 따로** 떴다. 원가 합계는 맞지만 독자가 보면 결함이다.
→ 총량으로 병합(우유 240ml). **원가 변동 0건** 확인. 재료줄 1,548 → 1,533.

### 3라운드: 판 결과 깨끗했던 것
| 각도 | 결과 |
|---|---|
| 브랜드 홈페이지 122개 전수 재확인 | **죽은 링크 0개** (조사팀이 곱창고 404라 한 건 서브페이지였고 루트는 정상) |
| 시판소스 23종 용량·단가 정합 | 0건 |
| 레시피 단계 수 | 3~5단계, 2단계 이하 0개 |
| dish 35개 브랜드 연결 | 빈 것 0개 |
| 메뉴 중복(같은 브랜드 같은 이름) | 0건 |
| 공정위 통계 연도 | 2023년 99 · 2022년 17 · 2021년 2 (연도폴백 정상 동작) |
| 카테고리 분류 | 2건 의심됐으나 둘 다 오탐(피자나라치킨공주=피자 맞음, 청년다방=떡볶이 브랜드) |
| 레시피 vs 재료표 양 불일치 | 25건 전부 오탐 — 식용유는 **의도된 차이**(레시피=붓는 양 / 재료표=흡수분 12%), 나머지는 정규식이 `떡볶이 양념 150g`을 `떡 150g`으로 오인 |

### healthcheck 확장 (총 35종)
이번에 추가: 고아 재료키(dish) · **gen_dishes.py DISHES 하드코딩 참조** · 한 메뉴 재료 중복줄 · 이름 같은 재료 여러 키 ·
**meta description · canonical · 중복 title · sitemap · robots · 고아 발행페이지**

## 2026-07-17 — 심층 발굴 5라운드 (성능·구조화데이터·자산)

### 🔴 JSON-LD 0/488 → 349개 메뉴에 Recipe 스키마 추가
**레시피 349개를 갖고도 구조화 데이터가 하나도 없어 구글 리치결과(재료·조리단계 표시)를 통째로 못 받고 있었다.**
- `recipe_jsonld()` 신설 → `@type: Recipe` · recipeIngredient · recipeInstructions(HowToStep) · recipeCategory
- ⚠️ **`offers`(가격)는 일부러 넣지 않는다** — 우리는 그 메뉴를 파는 주체가 아니다.
  남의 브랜드 메뉴에 가격 마크업을 하면 구글에 **우리가 판매자인 것처럼** 잡힌다. 원가·판매가는 본문에만 둔다.
  → healthcheck가 `offers` 존재를 **실패로 잡는다**(누가 나중에 넣지 못하게)
- 349개 전부 JSON 파싱 검증 통과

### 🔴 검수 탈락 로고 17개가 디스크에 그대로 (874KB)
사용자가 "틀리다"고 한 로고를 DB에서만 내리고 **파일은 안 지웠다.** 워커 배포 시 R2/Assets로 그대로 올라가
**틀린 브랜드 로고 파일이 서버에 존재**하게 될 뻔했다(명랑핫도그 음식사진 339KB 포함).
→ 삭제. 로고 파일 119 → 102개(DB 사용분과 정확히 일치). healthcheck에 "DB가 안 쓰는 고아 로고 파일" 추가.

### 성능
- `loading=lazy` 없는 img **358개 → 0** (+ `decoding=async`, `width/height` 명시로 CLS 방지)
- 발행 488개 · 8.0MB · 평균 15KB. 100KB 초과는 prices.html(123KB, 257종 데이터라 불가피) 하나뿐

### healthcheck 41종으로 확장
이번 추가: JSON-LD 없음 · JSON-LD 파싱실패 · **Recipe에 offers 있음** · loading=lazy 없는 img · 고아 로고 파일

### 배포 전 반드시 (⚠️ 잊으면 SEO가 통째로 깨진다)
도메인 확정 시 `SITE` 상수를 **4곳**에서 함께 변경:
`gen_sitemap.py` · `gen_preview_html.py` · `gen_dishes.py` · `gen_prices_page.py`
(지금은 `https://eolmanama.workers.dev`)

## 2026-07-17 — 심층 발굴 6라운드 (배포 정합)

### 🔴 schema.sql이 실제 DB와 크게 벌어져 있었다 — 배포하면 사이트 절반이 죽었다
HANDOFF에 **"D1 시딩 — data/eolmanama.db → D1 (schema.sql 기준)"** 이라 적혀 있는데, 그 schema.sql엔:
- **테이블 4개 누락**: `dishes` `dish_ingredients` `dish_menus` `brand_origins` → **요리사전 35개가 통째로 죽는다**
- **컬럼 11개 누락**: `ingredients.is_retail/wholesale_price/wholesale_basis` · `menus.store_cost/store_ratio`(사이트 헤드라인!) ·
  `menu_ingredients.composition/wholesale_cost` · `brands.logo_rejected/logo_pending`(틀린 로고 재수집 가드!)

ALTER TABLE로 컬럼을 늘려온 걸 schema.sql에 반영하지 않은 것. **손으로 관리하면 또 벌어진다.**
→ **`scripts/gen_schema.py` 신설 — 실제 DB에서 자동 생성.** schema.sql 상단에 "손대지 말 것" 명시.
→ healthcheck에 `schema.sql에 없는 실제 테이블/컬럼` 상시 검사 추가. 테이블 11/11 · 컬럼 0건 누락 확인.

### 🔴 내가 만든 버그: canonical 488개가 존재하지 않는 도메인을 가리켰다
5라운드에서 SEO를 붙이며 `SITE = "https://eolmanama.workers.dev"` 를 생성기 **4곳에 각각** 박았다.
그런데 `wrangler.toml`에는 이미 실제 주소가 있었다: `https://eolmanama-site-renderer.dndmotor1.workers.dev`
**canonical·sitemap 488개가 전부 없는 도메인을 가리키는 상태.** 그대로 배포했으면 SEO가 통째로 무효가 된다.
→ **`scripts/site_config.py` 신설 — wrangler.toml의 `SITE_ORIGIN`을 읽어 단일 출처로.** 생성기 4곳이 여기서 가져간다.
→ healthcheck에 `canonical 도메인 ≠ wrangler SITE_ORIGIN` 상시 검사 추가.

> **교훈: 같은 값을 4곳에 박으면 반드시 벌어진다.** 도매가 계산(단일출처 wholesale_cost)에서 배운 걸
> SEO 도메인에서 그대로 반복했다. 상수는 한 곳에서 읽어야 한다.

### 오탐이었던 것
컬럼 누락 경고가 재생성 후에도 남았는데 **내 검사기의 정규식 문제**였다 — ALTER로 붙은 컬럼은
`sqlite_master`의 CREATE 문 끝에 한 줄로 이어져 `^\s*(\w+)\s+TYPE` 패턴에 안 걸린다. 단어 경계 검색으로 바꿔 0건.

### healthcheck 44종
추가: schema.sql 테이블/컬럼 누락 · canonical 도메인 불일치

## 2026-07-17 — 심층 발굴 7라운드 (배포 실체·접근성)

### 🔴 배포는 0%다 — wrangler.toml 하나뿐
`workers/` 전체 파일 = **`site-renderer/wrangler.toml` 1개**. 나머지가 전부 없다.
| | 상태 |
|---|---|
| `src/index.js` (wrangler main) | ❌ **없음 → `wrangler deploy` 자체가 불가능** |
| `package.json` | ❌ 없음 |
| D1 `database_id` | ❌ `PLACEHOLDER_생성후_교체` → `wrangler d1 create` 필요 |

→ healthcheck가 이 셋을 매번 경고로 띄운다(⚠️ warn, 실패 아님 — 아직 착수 전이라).

### 🔴 `SITE_INDEXABLE = "false"` — 배포해도 검색 노출이 0이 된다
wrangler.toml에 이 값이 박혀 있다. 워커가 이걸 보고 noindex를 붙이면
**sitemap·canonical·JSON-LD·description을 다 만들어놔도 노출이 0**이다.
게다가 우리가 만든 robots.txt는 `Allow: /` 라서 **정면으로 모순**이다.
→ healthcheck에 "SITE_INDEXABLE=false인데 robots.txt는 Allow" 모순 검사 추가.
**배포 시 반드시 `true`로 바꿀 것.** 이 사이트의 자산이 검색 노출이다.

### 죽은 테이블 2개 (해는 없으나 D1에 그대로 생긴다)
- **`banners`** — 0행 · 참조 스크립트 **없음**. 배너는 `gen_preview_html.py`의 `ad_slot()`이 HTML로 처리 중이라 테이블이 무의미. 배너 슬롯은 `display:none`으로 정상 숨김 확인.
- **`brand_origins`** — 31행 있으나 `orig_map={}`으로 고정해 **화면에 안 나온다**(사용자 지시로 원산지 표시 제거). 출처 기록 없는 미검증 데이터라 되살리려면 근거부터 필요.

### 오탐 (판 결과 정상)
- **접근성 "빈 링크 텍스트 287개"** → 전부 로고 링크 `<a><img alt="샐러디 로고"></a>` 형태다. **img의 alt가 접근가능한 이름을 준다** → 브라우저 접근성 트리로 확인: **진짜 이름 없는 링크 0개**. 내 검출기가 태그를 모두 걷어내며 alt를 잃은 것.
- 버튼 접근가능한 이름 없음 0 · h1 여러개 0
- 카테고리 13개 slug·분류 정상 (치킨 20개 중 10개만 노출되는 건 메뉴 0개 브랜드라 정상)

### healthcheck 47종
추가: SITE_INDEXABLE vs robots 모순 · 워커 main 파일 없음 · D1 id 미설정

## 2026-07-17 — 메뉴 페이지 재설계 + 검색어 타깃

사용자 지적: "가독성·디자인이 직관적이지 않다" / "치킨 원가·짜장면 원가로 검색됐을 때 상단에 노출돼야 한다"

### 문제: 한 화면에 숫자 체계가 4개였다
`매장원가 606원` + `판매가 3,400원` + `집밥 880원` + 표의 `도매가 열`·`소매가 열`.
주인공이 없어 처음 온 사람이 뭘 봐야 할지 몰랐다. 헤드라인이 606원인데 옆의 3,400원이 더 크게 보였다.

### 고침
| 전 | 후 |
|---|---|
| 숫자 4개 나열 | **판매가 하나만 크게 + 원가율 막대 1개** |
| `원가율 37.4% · 차액 16,271원` | 막대 + `재료비 9,729원 · 37.4%` / `나머지 16,271원` |
| 🔴 "차액"이 **'남는 돈'으로 읽힘** (사이트명이 '얼마남아'라 더더욱) | **"나머지에는 임대료·인건비·배달수수료·본사 로열티가 들어갑니다. 남는 돈이 아닙니다"** 명시 |
| 면책 4줄이 본문을 밀어냄 | `<details>` "이 숫자는 어떻게 계산했나요?"로 접음 |
| 표 제목 `재료비 상세(집에서 만들 때·소매가)`인데 **도매가 열이 있는 모순** | `재료 구성` + "매장은 도매가, 집에서는 마트 소매가로 삽니다" 설명 |
| h1 아래 바로 카드 | **첫 문장이 답을 줌**: "교촌치킨 허니콤보는 26,000원. 재료비는 9,729원로 판매가의 37.4%입니다" |

### 🔴 잡은 버그: 클래스 이름 충돌
새 카드에 `.hero`를 썼는데 **홈 대문이 이미 쓰던 클래스**였다(`text-align:center`).
그 스타일이 먹어서 막대가 안 나오고 숫자가 가운데 정렬됐다. → `.pcard`로 분리.
> 조건문 `if ".hero{{" not in s`가 **기존 것을 보고 "이미 있네" 하며 CSS 삽입을 건너뛴** 것도 원인.

### 검색어 타깃 (title에 '원가')
`title`에 '원가'가 없으면 "치킨 원가" 검색에 안 잡힌다. 메뉴·브랜드가 이름만 있었다.
| | 전 | 후 |
|---|---|---|
| 메뉴 349 | `교촌치킨 허니콤보` | **`교촌치킨 허니콤보 원가`** |
| 브랜드 87 | `교촌치킨` | **`교촌치킨 원가`** |
| 카테고리 13 / 요리 35 | 이미 '원가' 포함 | 유지 |

**484개 전부 title에 '원가' 포함 확인.** description도 "판매가 26,000원, 재료비 9,729원으로 원가율 37.4%" 식으로
검색결과 스니펫에서 바로 답이 보이게 재작성.
