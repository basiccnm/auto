# 사고 기록 — 2026-07-21 ~ 07-28 (해결됨, 참고용)

> 이 항목들은 전부 **코드로 막혀서 재발하지 않는 것**들이다. CLAUDE.md가 매 세션 통째로
> 로드되면서 토큰을 많이 먹어, 최근·미해결 항목만 CLAUDE.md에 남기고 나머지를 여기로 옮겼다
> (2026-08-06). 왜 코드가 이렇게 생겼는지 궁금할 때만 열어보면 된다.

| 날짜 | 사고 | 어디서 막았나 |
|---|---|---|
| 07-21 | GA를 옆 폴더에 넣고 배포 | 전역 태그는 `scripts/theme.py` `HEAD_TAGS` 한 곳 |
| 07-24 | 엉뚱한 워커로 배포하고 "성공" 보고 | 배포 명령에 `--config` 필수 + 출력 워커명 눈으로 확인 |
| 07-25 | `.env` 의 `CF_API_TOKEN` 을 wrangler가 집어가 배포 실패 | 이름을 `OLMANAMA_CF_PURGE_TOKEN` 으로 격리 |
| 07-25 | 전체 캐시퍼지 직후 사이트가 `error 1015` | `cf_purge.py` 가 인자 없으면 **아무것도 안 함**, 전체는 `--all` 명시 |
| 07-25 | 식봄 캐시 단가 3,260행이 틀림("5kg2팩" 배수 미반영) | `pack_kg()` 수정 + `suspect` 플래그 + 카테고리 중앙값 이상치 탐지 |
| 07-25 | 재료 양 단위가 g/kg 뒤섞임 | DB는 g으로만 저장, 화면에서만 1,000g↑ kg 표기(`fmt_amt`) |
| 07-25 | 새 재료에 소매가를 안 넣어 원가 계산이 라인을 스킵 | `compute_line_costs.py` 가 "스킵된 라인 없음"을 매번 출력 — 그 줄을 확인할 것 |
| 07-25 | g 합계만 보고 "라면 20g·핫도그 50g" 오판 | 양 검수는 `ml`·`개`·`세트` 단위를 **반드시 함께** 볼 것 |
| 07-26 | **빈 `_dist` 를 배포해 사이트 전체 404** | `scripts/deploy.py` 가 파일 수를 세고 미달이면 배포를 막는다 |
| 07-26 | `_headers` 규칙이 겹쳐 `Cache-Control` 이 3개 붙은 잘못된 헤더 | 경로별로 **딱 하나만** 매칭 (`_headers` 는 덮어쓰기가 아니라 이어붙이기) |
| 07-26 | 자산에 `?cb=` 쿼리를 붙여 404 를 "사이트 죽음"으로 오판 | 캐시 우회는 `Cache-Control: no-cache` 헤더로만. 자산에 쿼리 금지 |
| 07-27 | 상세페이지 본문에서 **메뉴명을 추측**해 옵션 라벨(`양념`·`구이`·`세트`)이 잡혀 연결률 2% | 목록 페이지를 **가격 줄 기준 블록**으로 자른다 (`brand_menu_list.py`) |
| 07-27 | 소매 트랙을 다 만들고 **원가 계산에서 안 읽어** 쿠팡 상품을 바꿔도 원가가 그대로 | `compute_line_costs.price_of()` 가 `ingredient_retail` 을 1순위로 읽는다. **`pack_g` 없으면 못 쓴다** |
| 07-27 | 소매에 값이 싸다고 **다른 물건**을 붙임(포마스 혼합유·리코타 치즈·알룰로스) | 자동 판정 금지. ①물건 맞나 ②최소규격 ③총지출 최저 ④못찾으면 비운다 |
| 07-27 | 쿠팡 용량을 3초 간격으로 몰아 읽어 **11건에서 차단** | 6초 간격 + 브랜드별로 5~10건만. 한 건씩 즉시 저장해 끊겨도 이어감 |
| 07-27 | **소매값이 없으면 도매 라인까지 통째로 날아감** — 머리고기 4개 순대국의 매장원가가 과소인데 출력은 "✅ 스킵된 라인 없음" | `compute_line_costs.wholesale_line()` 이 소매와 **무관하게** 먼저 계산한다. 값 없는 라인은 `⚠️ 소매값이 없어 스킵` 으로 반드시 출력 |
| 07-27 | `manual_price=0` 인 재료가 라인가 **0원**으로 조용히 들어가 소매 원가를 낮춤 | `if not p:` 로 0 도 "없음" 취급 (`compute_line_costs`) |
| 07-27 | bhc 5개 메뉴가 **BBQ 와 같은 파우더·양념소스**를 써서 두 브랜드 원가가 같아짐 | 브랜드 전용으로 분리(`breading_bhc`·`breading_bhc_bburing`·`sauce_yangnyeom_bhc`). 근거는 공식 **알레르기 표시**가 파우더 종류를 갈라준다 |
| 07-27 | 브랜드를 숨겼는데 **옛 페이지 203장이 라이브에 계속 서빙** — 한신포차 `menu_478.html`(새벽 1시 파일)이 DB에 없는 재료명을 뿌리고 있었다 | `scripts/preview_orphans.py`. **브랜드를 켜고 끌 때·재료명을 바꿀 때마다** 돌린다 |
| 07-27 | 화면 재료명에 상표가 그대로(`뫼루니 베이직 치킨용파우더 5kg`) — 복사해서 쿠팡에 붙이면 0건 | `ingredients.display_name`·`page_slug` 신설. 마스킹 규칙은 `scripts/namemask.py` **한 곳**. 본문만 고치면 **JSON-LD로 새어나간다** |
| 07-27 | 소매 상품명을 다듬어 **용량 표기를 지워** `pack_g`가 비고 원가가 옛 `manual_price`로 조용히 폴백 | `ingredient_retail.name_raw`에 원문 보존. `retail_pick_sized.py`로 **고를 때 용량 있는 후보만** 채택 |
| 07-27 | 폰 용량 스크립트가 대상을 0건으로 봄 — 조건이 `pack_g IS NULL`이라 **`0`인 행을 놓쳤다** | `(pack_g IS NULL OR pack_g = 0)`. 개수를 셀 때도 `pack_g > 0`으로 |
| 07-27 | 고아 203장을 지우니 파일이 651장이 되어 **정상 배포가 하한선(800)에 막힘** | `deploy.py` 하한선 400/300. 이 검사의 목적은 **빈 `_dist` 차단**이지 장수 고정이 아니다 |
| 07-27 | 식봄 정육 **카테고리 지도(`meat_price_table.py`)가 이미 있는데** 미트박스를 쓰고 식봄을 검색으로 돌려 임박상품·스파게티소스를 최저가로 잡음 | **`데이터소스와스크립트.md` 를 작업 전에 읽는다.** 고기도 식봄, 식봄은 카테고리(nid) |
| 07-27 | 쿠팡 검색 결과를 `productData`·`productName` 으로 읽어 «후보 0건» 을 반복 | 정본은 `{keyword,count,items:[{name,price,rocket,id,url}]}`. `coupang_show.py` 로 통일 |
| 07-27 | 이름 자동 매칭이 `BBQ 양념치킨 24,500` 에 **1,000원짜리 소스**를 붙임 | 가격 ±35% 안에서만 후보. 못 붙으면 **비운다**. 판매가는 공식 페이지 직접 읽기가 정본 |
| 07-27 | `menu_link_suggest` 가 **커서 재사용**으로 첫 브랜드만 보고 끝남(출력이 비었다) | 바깥 루프는 반드시 `.fetchall()` |
| 07-27 | **`gen_ingredient_pages` 가 어느 파이프라인에도 없었다** — 재료 페이지 326장이 낡은 채로 배포됨. 브랜드를 숨겨도 `price_*.html` 이 지워진 `menu_*.html` 을 계속 링크(죽은 링크 33건) | `daily_update.py` · `admin_pipeline.py` **양쪽** BUILD 목록에 넣었다 |
| 07-27 | `preview_orphans` 가 «`page_slug` 가 있으면 살아있다»로 판정 — page_slug 는 670종인데 실제 생성은 326종이라 **옛 페이지가 영원히 남았다**(`price_양념치킨소스.html`) | `live_ing` 을 `gen_ingredient_pages` 의 rows 쿼리와 **똑같은 조건**으로 바꿨다 |
| 07-27 | `meat_price_table` 의 소 «불고기용 nid 632» 가 실은 **`양념육 > 불고기용`(완제품)** — 최저가가 `맛술 혼미린 1.8L` 로 잡혔다 | 카테고리 **이름을 받아서** 확인(`foodspring_cat_names.py`). 진짜 불고기감은 `세절 수입산 > 소전각` **613** |
| 07-27 | 도시락 브랜드를 **원물로 쪼개려다** 원가율 8~13% (재료가 통째로 누락) | 본도시락·한솥도시락 **비공개.** 레토르트 원팩으로 들어오고 구성이 끝없이 늘어난다 |
| 07-27 | 피자 L 인데 도우가 `425/413/413.194/400/372`, 치즈가 `315.714/260/259.722` — **소수점 3자리는 비율 계산값**이지 실측이 아니다. 파파존스에서 정한 기준을 다른 브랜드에 안 옮겼다 | `scripts/pizza_norm.py` — L = 도우 400·치즈 260·소스 120·페퍼로니 70. `양배추`·`당근` 은 옛 «신선 채소» 자동분해 잔재라 제거(`mi_removed` 보관) |
| 07-27 | `build_dist` 두 개가 동시에 돌아 `[WinError 32] 파일 잠김` | 빌드는 **한 번에 하나만.** 백그라운드 재생성 중에 앞에서 또 빌드하지 말 것 |
| 07-27 | `brand_menu_list` 가 **홈페이지를 렌더링해놓고 파싱을 안 했다** — 홈을 `seen` 에 넣고 다른 페이지만 훑어서 **메뉴가 홈에 있는 사이트가 통째로 0쪽**(네네·도미노) | `_do()` 에서 홈 DOM 도 `parse_list` 에 물린다 |
| 07-27 | 가격이 `23,000` / `원` **두 줄로 쪼개져** 오는 사이트를 전부 0건으로 봤다(`<span>` 으로 감싼 곳) | `to_lines()` 끝에서 «숫자 줄 + 원 줄» 을 다시 붙인다 |
| 07-27 | 메뉴명 자리에 **설명문**이 들어갔다(가격 바로 위 줄을 집어서) | 창 안의 후보 중 **띄어쓰기 적은 순 → 짧은 순**으로 고른다. 메뉴명은 짧고 설명문은 길다 |
| 07-27 | 백그라운드 수집(크롬 48개)이 도는 중에 `build_dist` 를 돌려 **`_dist` 가 137개로 무너짐**(로고 0개) | `_preview` 는 멀쩡했다. **수집·빌드를 겹치지 말 것.** 배포 전 `_dist` 파일 수를 눈으로 확인 |
| 07-27 | 근거 없던 «조사 고정단가 (출처 비공개)» 5,100원/kg 을 국산 13,200원으로 올렸다가 **지코바가 브라질산**인 걸 놓쳤다 | 재료를 **원산지별로 쪼갠다**(`chicken_boneless` 국내산 / `chicken_boneless_import` 수입). 브랜드마다 원산지가 다르면 한 재료로 표현할 수 없다 |
| 07-27 | 치킨무 소매를 빼려고 `ingredient_retail.status='none'` 으로 넣었더니 **90개 메뉴의 소매 총액이 통째로 재계산 안 됨**(360→275개) | 소매에서 뺄 재료는 **`status='store'`**(매장전용). `compute_line_costs.store_only()` 가 그것만 보고 `all_priced` 를 안 깬다. `'none'` 은 «소매값 없음» 이라 뜻이 다르다 |
| 07-27 | 엽떡 3메뉴에서 **기본 치즈를 뺐다** — 사이드에 «모짜치즈 3,000원» 이 있는 걸 "기본엔 없다"로 읽었다. 대표 정정: *"기본으로 엽떡에 치즈가 들어가"* | **사이드에 있다 ≠ 기본에서 빠진다.** 추가 토핑은 «기본에 더 얹는 것» 이다. 기본 구성을 뺄 땐 **빠졌다는 근거**가 필요하다(있다는 근거로 빼지 말 것) |
| 07-28 | `theme.py` 의 GNAV 를 «순위»→«식자재마트» 로 바꾸고 **생성기 일부만 돌려** 415장이 옛 메뉴로 남았다 — 페이지를 옮길 때마다 메뉴가 바뀌어 보였다 | **`theme.py` 를 고치면 생성기를 전부 돌린다.** CLAUDE.md 「전체 재생성」 한 줄 |
| 07-28 | 표준 탭으로 통일하다 **브랜드 원본 카테고리를 덮어씀** — BBQ `바이젠 500cc`(원본 「음료」)가 **치킨** 탭으로. `_ALIAS` 에 «음료» 키가 없어 무시되고 «치킨집이니 치킨» 폴백이 이겼다 | **원본이 1순위**로 뒤집었다 → `브랜드메뉴판등록방식.md`. 표준 탭은 원본 없는 브랜드용 폴백 |
| 07-28 | 범용 수집기 10개가 같은 테이블에 적재 — **출처 없는 행 113건**(BBQ `후라이드 23,000` 이 어디서 왔는지 추적 불가) · **문구가 메뉴명으로 101건**(`새로 나왔어요! 46,000원`, 푸라닭 14개 전부 홍보문구) · **카테고리가 메뉴로**(BBQ `피자&버거 5,000원`) | **브랜드 1곳 = 전용 스크립트 1개.** 적재는 `brand_menu_store.replace_brand()` 만 — 자기 `brand_id` 밖 금지 + `collector` 기록 |
| 07-28 | 카테고리를 **이름**으로 키 잡았다가 bhc 「치킨 > 뿌링클」과 「BHC시그니처 > 뿌링클」이 충돌 | 키를 **경로**(`치킨/뿌링클`)로. `UNIQUE(brand_id, path)` |
| 07-28 | bhc 하위 카테고리를 `/categories/{하위idx}/products` 로 받으려다 **전부 0개** | 하위별 상품은 **상위 목록** 각 상품의 `cateSortList.sortKey` 에 있다. `sortKey % 10000` = 하위 안에서의 자리 |
| 07-28 | **캐시 퍼지가 조용히 아무것도 안 했다** — 730경로를 한 줄에 넘겨 **Windows 명령줄 8,191자 제한**을 넘겼다. 배포는 성공인데 엣지엔 옛 페이지가 남아 눈치채기 어렵다 | `publish.py` 가 **150개씩 끊어** 부르고 묶음마다 성공을 확인한다 |
| 07-28 | `build_dist` 가 잠긴 파일에 죽었다 — `_force` 가 `PermissionError` 만 재시도해서, 백신이 훑는 중이면 마지막 `rmdir` 에서 **OSError 145(디렉터리가 비어 있지 않음)** 로 실패 | `_force` 가 **OSError 전체**를 재시도(0.3~5초) |
| 07-28 | JSON 대조를 **메뉴 수로만** 해서 «가격만 채워진 갱신» 을 통째로 놓쳤다 — 호식이지두마리치킨이 JSON 가격 24개인데 DB 0개로 라이브에 나가 있었다 | `brand_status.py` 가 **메뉴 수·가격 수를 둘 다** 본다 |
| 07-28 | 사이트맵을 07-19 이후 **구글이 9일간 다시 읽지 않아** 새로 만든 392장이 색인 밖에 있었다 — 서치콘솔 «발견됨-색인 안 됨» 456장 | 배포 뒤 `python scripts/gsc_ping.py` 로 재제출 |
| 07-28 | 사이트맵 700장을 **10병렬로 라이브 확인**하다 429 레이트리밋 — 라이브를 내가 두드렸다 | 검증은 **`_dist` 파일 존재 확인**으로. 라이브 동시 요청 금지 |
