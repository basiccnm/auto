> ⬆️ 이 문서는 `올마나마\인수인계-2026-08-02.md` 로 통합됐다. 새 세션은 그쪽을 읽는다.

# 인수인계 — 2026-08-01 (올마나마)

> 이 세션이 한 일과 **다음 세션이 이어받을 것**. 현재 공개 브랜드 103곳 · 브랜드메뉴 9,574개(가격 7,634) · 요리 74개.

## 🚨 배포 상태 — 마지막 변경분 **미배포 가능성**
- 세션 후반에 대표가 *"배포하지마 — 다른 세션에서 홈페이지 작업 중"* 이라 함.
- **미배포일 수 있는 변경:** ① 요리 레시피 13개 수정 ② 재료 4개 신규등록 ③ 배달가 배너·가격순 정렬(gen_preview_html) ④ 서브웨이·KFC 신규.
- **다음 세션이 다른 세션 작업 끝난 걸 확인한 뒤** `python scripts/publish.py --apply` 로 한 번 배포하면 다 올라간다. (배포 전 `_dist` 충돌 없나 확인 — build_dist 겹치면 무너진 전례)

## ✅ 오늘 완료

### 1. 배달가 수집 (쿠팡이츠 화면판독) — 30여 브랜드
- 파이프라인: `ce_search_enter`(검색·진입) → `brand_coupangeats.py`(원본 저장) → `brand_coupangeats_parse.py`(가공) → `delivery_price_match.py`(골격 매칭) 또는 `delivery_to_skeleton.py`(골격 교체)
- 채운 브랜드: 파리바게트·뚜레쥬르·던킨·폴바셋·맥도날드·얌샘·스타벅스·공차·더벤티·미스터피자(홈페이지)·노브랜드버거·보배반점·명륜·샐러디·원할머니·자담·역전우동·죠스·홍콩반점·응급실·바르다·소림마라·귀한족발·육수당·한신포차·60계·채선당·하남돼지집·포케올데이·고피자·탕화쿵푸·춘리마라탕 등
- **주소를 쌍문역으로 바꿔** 강남에 없던 60계·채선당·하남돼지집·포케올데이까지 수집(쿠팡이츠 앱, ADBKeyboard로 한글검색)
- 함정·규칙은 메모리 [[coupangeats-collection-pitfalls]]·[[coupangeats-delivery-price-collection]]·[[delivery-price-name-matching]]

### 2. 신규 브랜드 2개
- 서브웨이(subway, 168개)·KFC(kfc, 104개) — 둘 다 버거(패스트푸드) 업종, 배달가. **published=1 로 공개됨.**

### 3. 요리(대카테고리 레시피) 정밀 조사·수정
- 74개 전수 4묶음 조사 → **FIX 13개** 반영(로제떡볶이·짬뽕·마라탕·순대국밥·뼈해장국·설렁탕곰탕·김밥·돈까스·족발·삼계탕·갈비탕·미역국·잔치국수). 나머지는 완제양념/충실이라 OK
- 조사표: `레시피정밀조사-2026-08-01.md` / 파이프라인: `scripts/dish_recipe_apply.py` + 메모리 [[dish-recipe-fix-pipeline]]
- 재료 4개 신규등록: 소갈비(수입) 11,090 · 들깨가루 6,380 · 삼계탕재료(한방) 13,000 · 부추 2,117 (원/kg, 식봄 도매)
- `compute_line_costs.py` 재계산 완료(스킵된 라인 없음)

### 4. 화면 개선 (gen_preview_html)
- 배달로 받은 브랜드 메뉴판 위에 **「배달앱 가격 — 매장보다 비쌀 수 있고 지역·지점마다 다름」 배너**(`.dlvnote`)
- **가격 확인된 메뉴를 위로 정렬**(`_price_first`), 「가격 확인 중」은 아래
- 배달 프로모션 배지 화면에서 제거(`_disp_name`: `[특급할인]`·`[MD]`·`(24시)` 등, DB는 보존)

### 5. 공통모듈에 올림
- `공통모듈\coupang_eats\` (ce_search_enter·brand_coupangeats·brand_coupangeats_parse·README) — 원본 보관, 쓸 땐 복사

## 📌 남은 일 (우선순위)
1. **배포** (위 배포 상태 참고 — 다른 세션 확인 후 1회)
2. **삭제한 브랜드**: 미카도스시·핵밥·와플대학·서가앤쿡 (재등장 금지)
3. 배달가 낮은 곳은 대부분 정상 — 던킨(39/205)·파바(178/502) 등은 **공식 골격이 크고 배달에 없는 항목**(캔음료·아침메뉴·특수품)이라 낮은 것. 억지로 안 채워도 됨
4. 요리 레시피는 FIX 13개만 손봄 — 나머지 61개는 「OK 판정」(완제양념/충실). 추가 손볼 필요 생기면 조사표 갱신
5. `dish_recipe_apply` 파서 이름상한을 40→60자로 올림(긴 세트명). `brand_coupangeats_parse`도 60. 공통모듈 것과 동기화 필요하면 복사

## ⚠️ 이 세션에서 낸 실수 (반복 금지)
- 인증우회 삽질(와플대학 비버웍스 API) — 제3자 인증 우회는 안 함. 화면판독으로 대체
- 병렬로 두 에뮬 동시 수집 → adb 충돌로 48줄만 남음. **수집은 하나씩**
- 수집 프로세스 안 끝났는데 중간 원본으로 파싱(KFC 66→104). **줄수 멈춘 것 확인 후 파싱**
- 이름매핑 오매칭(부추→물만두·떡볶이양념→고로케반죽·물엿→물) — 반영 전 `→ 우리이름` 눈으로 확인
- 브랜드 메뉴(원가용)와 요리(대카테고리 레시피)를 혼동 — **둘은 다른 트랙**
