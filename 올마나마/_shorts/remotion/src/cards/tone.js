// 🎨 여성향 톤 표준 (2026-07-25 확정) — 모든 편이 이걸 가져다 쓴다
//
// 🔴 왜 바꿨나: "지금 스타일이 너무 남자·게임 취향"이라는 지적.
//    원인은 ① 아이소메트릭 배경 ② 검은 판 + 형광 노랑 ③ 각진 폰트 였다.
//    영향이 큰 순서대로 고쳤다 — 판 색 > 강조색 > 배경 > 볼터치.
//
// 🔴 배경 이미지를 안 쓴다. 아이소메트릭 매장 그림 자체가 게임 문법이라 뺐다.
//    대신 격자(GridBg) + 주방도구(ToolScatter)를 코드로 그린다 → 편마다 배경 뽑을 일이 없다.
//
// 🔴 핑크가 과해 보일 수 있지만 **구매 결정권이 여성에게 있다**(2026-07-25 판단).
//    다만 채도를 낮춰 유치해지지 않게 한다 — 형광 핑크가 아니라 살구빛.
export const TONE = {
  // 배경 — 살구·크림. 편마다 음식 색에 맞춰 미세 조정해도 되지만 기본은 이것
  bg: "#F7E2D8",
  grid: "rgba(175,120,95,.34)", // 🔴 .15는 안 보인다

  card: "#FFFFFF",       // 판은 흰색 + 그림자(테두리 없음)
  shadow: "0 14px 40px rgba(170,140,120,.16)",

  // 🔴 **파스텔 글씨 금지**(2026-07-25 반복 지적). 파스텔은 배경에 쓰고, 글씨는 진하게.
  //    "흐리멍텅해 보여" — 배경이 살구색이라 연한 갈색 글씨는 거의 안 읽힌다.
  // 🔴 **글씨는 갈색 안 쓴다. 검정이다**(2026-07-25 지시).
  //    살구색 배경 + 갈색 글씨는 흐리멍텅해 보인다. 색은 배경·강조에만 쓴다.
  ink: "#111111",        // 본문
  sub: "#111111",        // 보조 설명 — 크기로만 위계를 준다
  point: "#E05575",      // 강조 — 코럴. 연하면 숫자가 안 튄다
  gold: "#DDA05A",       // 숫자 보조
  line: "#F3EAE3",       // 구분선

  ribbon: "#F0AFBC",     // 리본 배지 — 채도 낮춘 핑크
  softbox: "#FFF4EC",    // 강조 박스 바탕
};

// 편별 배경 변주 — 음식 색과 같은 계열로 잡는다(보색 쓰면 음식이 붕 뜬다)
export const BG_BY_FOOD = {
  마라탕: "#F7E2D8",   // 빨강 → 살구
  빙수:   "#FBEDDF",   // 노랑 → 크림
  피자:   "#F7E6D5",   // 주황 → 베이지크림
  치킨:   "#F9E4E0",   // 갈색 → 연한 핑크
  디저트: "#F6E7EE",   // → 연한 로즈
};
