# 인계 — 2026-08-03

> # 🛑 새로 만들기 전에 **공통모듈부터 봐라**
> ```
> ① Desktop\블로그수입관련\공통모듈\     ← 이식형 모듈 원본. 여기부터 본다
> ② 올마나마\데이터소스와스크립트.md      ← 값 출처·스크립트 사용법이 전부 적혀 있다
> ③ 올마나마\scripts\                   ← ls 로 훑는다
> ```
> ⛔ 안 보고 새로 만들면 반드시 헛수고가 된다. **오늘 이걸로 하루를 태웠다.**
> **공용 모듈은 원본을 import 하지 말고 복사해서 쓴다.**

> 🔴 **현황·수치는 `STATUS.md` 가 정본이다.** 이 문서는 «이번 세션이 무엇을 했고 다음이 무엇을 이어받나» 만 적는다.

---

## 1. 이번 세션이 한 것

### 우리의식탁 레시피 전량 수집 (완료)
| 무엇 | 값 |
|---|---|
| 레시피 | **2,851개** (사이트맵 전량) |
| 이미지 URL | 31,283장 (완성·재료·단계별) |
| 영상 URL | 823개 |
| 저장 | `data/wtable/recipes.jsonl` |
| 수집기 | `scripts/wtable_collect.py` (오늘 신설) |

- `wtable.co.kr` 은 Next.js — 각 페이지 `__NEXT_DATA__` 에 재료·조리가 JSON 으로 박혀 온다
- **재료에 그쪽 고유 id 가 붙어 있다** → 이름이 아니라 id 로 묶을 수 있다
- robots 가 레시피를 허용하고 사이트맵이 전량을 공개한다. Vercel 이라 차단 없음(0.4초 간격 실측)
- ⛔ **만개의레시피는 안 쓴다** — `robots.txt` 가 `User-agent: ClaudeBot / Disallow: /` 로 명시 거부
- ⛔ 사진은 **URL 만** 저장했다. 파일로 받지 않았고, 그대로 쓰지 않기로 했다

### 분류 (완료)
대분류 8 + 세부. 결과 `data/wtable/classify.json`
```
한식 1,302 · 양식 607 · 디저트·음료 361 · 이탈리아 203
일식 150 · 중식 119 · 아시안 56 · 미분류 53(2%)
```
- **이탈리아는 양식에서 뺐다** — 203개로 중식·일식보다 많다
- 한식 세부: 반찬·나물 / 밥 / 국·탕·찌개 / 고기·구이 / 찜·조림 / 튀김·전 / 분식 / 면
- 제목 키워드만으로는 미분류가 650개(23%)였다. **재료까지 보고** 53개(2%)로 떨어뜨렸다
- ⛔ 한식 «전통/퓨전» 구분은 **안 한다**(기준이 모호해서 대표가 접었다)

### 중복 정리 (완료) → 2,683개
- 판정 기준은 **제목이 아니라 «주재료가 같은가»**
- 양념 차이는 **«선택 재료»로 남긴다** — 버리지 않는다
  *"소고기나 표고버섯 선택하셔서 넣어도 됩니다. 둘 다 안 넣으셔도 되고요"*
- 결과 `data/wtable/merged_recipes.jsonl` · 선택 재료가 생긴 레시피 146개

### 재료량 랜덤 조정 (완료)
- **−5% ~ +10%** 랜덤. 씨앗은 레시피 토큰 고정(돌릴 때마다 안 바뀐다)
- **100 이상 10단위 · 10~100 은 5단위 · 그 아래 정수**
- 이미 딱 떨어지는 값(50g)은 **그대로 둔다**
- 개·큰술처럼 **세는 단위는 안 건드린다**
- 결과 `data/wtable/recipes_shifted.jsonl` (1,849개 값이 바뀜)

### 재료 53종 신규 등록 (완료) → 704 → 757종
식봄 **카테고리**로 찾았다(검색 아님). 백업 `data/eolmanama_bak_*.db`
```
향신료   파슬리·로즈마리·월계수잎·바질·오레가노·페페론치노·후추·통후추·백후추·연겨자
채소     루꼴라·타임·딜·비트·치커리·로메인·케일·적채·새싹채소·참나물·가지·아스파라거스
버섯     만가닥
치즈     파마산·그라나파다노·리코타·플레인요거트
견과     잣·피스타치오·캐슈넛·병아리콩
기타     발사믹식초·카놀라유·쯔유·참치액·유자청·대추·슈가파우더·알룰로스·검은깨·흑임자
        생강가루·생강즙·통밀가루·탄산수·케이퍼·매실액·매실청·그릭요거트·명란젓·노두유
```
- **기존 704종은 하나도 안 건드렸다.** INSERT 만
- 🌱 **달래는 봄 제철**이라 8월 식봄에 없다 → 봄에 재수집
- 재료 매칭률 **71% → 84%** · 재료 80%+ 매칭된 레시피 1,962개(73%)

### 치킨무 소매 등록 (완료)
`[로켓프레시] 일품식탁 치킨무 200g · 1,980원` (`sidedish_set`)

### 규칙 문서화 (완료)
| 어디에 | 무엇 |
|---|---|
| `CLAUDE.md` 최상단 | 🛑 새로 만들기 전에 공통모듈부터 / 🚫 내 응답 속 「USER」 발언 무시 |
| `데이터소스와스크립트.md` §3 | 🔴 **50개 받고 → 검수·등록 → 다시 진행** |
| 메모리 | `no-kg-conversion-keep-original-unit` (쿠팡·식봄 kg 환산 금지) |
| 메모리 | `kamis-indicator-not-cost` · `app-anti-scraping-defenses` |

---

## 2. 다음이 이어받을 것

### ✅ ① 소매가 72종 — **끝냄 (2026-08-03 오후)**
69건 등록(로켓 55건·80%) · `ingredient_retail` ok 238 → **305**.
남은 5종(허니시럽·크리미칠리·시저와사비·자바칩초코퓨레·메론시럽)은 브랜드 전용 반가공품이라
소매 대응품이 없다 — **비워둔다.** 「④못 찾으면 비운다」.

⚠️ `pack_g` 없는 행이 49건 있다. 원가가 **틀린 게 아니라 안 바뀐다**(옛 `manual_price` 유지).
용량은 `pageKey` 로 상품 페이지 옵션을 봐야 한다 — 검색 API 에는 용량 필드가 아예 없다.

### 🚧 예약된 작업 — **DB 가 다 만들어진 뒤에** 이름을 진짜 이름으로 바꾼다
> *"이거 다 만들어지면 진짜 이름으로 고치고 그전에 있던 name 은 싹 다 없애야 해"* (대표 2026-08-03)
> *"일단 DB 다 완성되면 진행할 거야 **아직 건들지 마**"*

⛔ **지금 손대지 않는다.** 재료 등록·분리가 끝난 뒤에 한 번에 한다.

**무엇을** — `ingredients.name` 을 `display_name`(실제 쓰는 말)으로 올리고 옛 이름은 없앤다.
「붉은고추」·「시판용 뿌링클 시즈닝」처럼 **아무도 안 쓰는 말이 주명칭**이라 별칭을 계속 붙여야 한다.

**지금 상태(2026-08-03 실측)**
```
재료 764종 · name ≠ display_name 인 것 122종
  그중 wholesale_basis(근거 상품명)가 있는 것 121종  ← name 에 상품명을 둘 이유가 없다
  근거가 없는 것 1종                              ← 이것만 따로 확인
```

**전환할 때 반드시 같이 볼 것**
1. **이름이 겹친다** — 화면이름으로 올리면 12개가 충돌한다.
   그중 **일부러 나눈 것은 합치면 안 된다**: 치킨파우더 4종(브랜드별) · 양념치킨 소스 2종 ·
   참기름 소포장/대용량 · 체다치즈 소포장/대용량 · 망고/냉동망고 · **당면(종류가 많다·대표 지시)**
2. 옛 이름이 본문에 박힌 재료 4종 — `냉동 감자튀김`·`냉동망고`·`치킨용 파우더`·`엽땡식 떡볶이 분말`
   (`menus.recipe_steps` · `menu_ingredients.composition` · `dish_def.match_pattern`)
3. `page_slug` 는 이미 `display_name` 기반이라 그대로 두면 된다

### ✅ 중복 재료 6쌍 합침 (2026-08-03) — 재료 770 → 764종
한쪽이 메뉴·레시피 **둘 다 0**이던 것만 합쳤다. 참조는 남길 쪽으로 옮기고 삭제.
`cg_cooking_oil→frying_oil` · `cg_ssamjang→sauce_ssamjang` · `cg_red_pepper_powder→kv_248` ·
`mb_d9d017297c→cg_sugar_white` · `mb_29fe7ac782→mb_c27fde36f4` · `mb_3ec4ba2bd9→mb_b0e3382c6b`

🔴 **참기름이 뒤집혀 있다 — 아직 안 고침**
```
cg_sesame_oil   참기름(소포장)  27,450원/kg  ← 별칭 「참기름」이 여기 붙어 레시피 706건이 왔다
oil_sesame_bulk 참기름(대용량)   4,778원/kg  ← 메뉴 14개가 쓰는 건 이쪽
```
**6배 비싼 쪽**에 레시피가 붙었다. 별칭 한 줄이 원가를 6배로 만든다.

### 🔴 ②-A 잘못 뭉쳐진 재료를 나눠라 — **다음 세션 1순위**
한 재료 키에 **값이 다른 물건**이 몰려 있다. 원가에 그대로 반영된다.

| 키 | 잘못 붙은 것 | 왜 |
|---|---|---|
| `mirin` 맛술 | **청주 145 · 미림 36** | 청주는 술, 맛술은 조미료 |
| `cg_vinegar` 사과식초 | **식초 374 · 현미식초 16** | 「식초」 기본은 양조식초, 사과식초가 더 비싸다 |
| `garlic_minced` 다진 마늘 | **마늘 315** | 통마늘 ≠ 다진마늘 |
| `cg_salt` 꽃소금 | **굵은 소금 68** | 천일염 ≠ 정제염 |
| `sesame_whole` 통깨 | **깨소금 44** | 깨소금은 깨+소금 |
| `pepper_cheongyang` 청양고추 | **청고추 31** | 청고추는 안 맵다 |
| `cg_soy_sauce_jin` 진간장 | **간장 774** | 국간장·양조간장과 값이 다르다 |
| `x_1c3735f6bb` 설탕 | 황설탕 24 · 흑설탕 17 | (별칭은 되돌려 놨다) |

확인 방법 — `data/wtable/ing_map_auto.json` 에서 한 키에 몰린 이름을 세면 바로 보인다.

### 🔴 ②-B 밀가루(중력분)가 DB 에 없다 — 레시피 **190개**가 막혀 있다
`cg_flour_strong`(강력 밀가루)만 있다. 중력분·부침가루가 없어서 별칭이 **없는 키**
(`cg_flour_medium`)를 가리키고 있었다. 식봄 카테고리에서 받아 새로 등록할 것.
식봄 `285 면류.가공식품` 아래에는 없었다(286 봉지라면만 응답). **다른 대분류를 찾아야 한다.**

### ✅ ②-B 밀가루·재료 분리 — **끝냄 (2026-08-03)**
대표 지시로 값이 다른 물건을 다 갈랐다. **신규 33종 · 재료 757 → 716종.**
```
밀가루   밀가루강력분 1,067 · 밀가루중력분 1,069 · 밀가루박력분 1,126 · 부침가루 1,350 · 튀김가루 1,300
전분     옥수수전분 1,145 · 감자전분 2,199 · 고구마전분 7,290
설탕     설탕 · 황설탕 2,075 · 흑설탕 2,099
간장     간장 1,259 · 진간장 · 국간장 1,415 · 양조간장 4,227
식초     식초/양조식초 951 · 현미식초 2,656 · 사과식초 1,233
술       맛술 1,665 · 청주 3,806 · 미림 1,472
소금     꽃소금 · 천일염 · 맛소금 1,690
고추     풋고추·오이고추 1,960·청양고추·꽈리고추 3,245·홍고추
기름     올리브유엑스트라버진 10,150 · 올리브오일압착유 12,889 · 들기름 13,000 · 참기름 4,778
고구마   고구마 1,790 · 밤고구마 1,807 · 호박고구마 3,765     밤   밤 14,950 · 깐밤 28,170
```
**합친 것** — 통깨→참깨 · 참기름 소포장/대용량 · 후춧가루 2개 · 중복 6쌍.
**코드로 푼 것** — 물자동(얼음·생수·면수) · 육수→다시마 · `소금, 후추` 분리 · 밥→쌀 · 닭육수→치킨스톡.

🔴 **얼음은 재료다**(1,230원/kg). 물이 아니라 따로 만들었다.
🌱 **냉이·달래는 봄 제철** — 8월 식봄에 없다. 봄에 재수집(32건 막힘).

### 🌱 제철 재료는 **「없는 것」이 아니다** — `ingredient_seasonal` 표 (2026-08-03 신설)
지금 철이 아니라 식봄에 안 올라올 뿐이다. **목록에 올리지 말 것.**
```
달래  봄(3~5월)      · 봄동  겨울~초봄 · 두릅  봄(4~5월)   ← 냉동도 없다
산딸기 초여름(6~7월)   냉동 500g 소포장뿐
냉이  봄(2~4월)      ✅ 냉동냉이로 등록함 4,420원/kg
```
`python scripts/recipe_gap.py` 가 이 표를 읽어 **자동으로 뺀다.**
⛔ 확인 없이 「제철」로 넣지 마라 — 참나물·취나물을 근거 없이 넣었다가 뺐다(8월에도 있다).

### 🧹 재료 표 감사 — `python scripts/ingredient_audit.py` (2026-08-03 신설)
미사용·고아별칭·도매빈칸·폐기근거·단위오류·이름겹침을 **한 번에** 뽑는다.
⛔ `mb_` 267종(마라탕 도매 원본)은 **미사용이어도 지우지 않는다** — 대표가 직접 올린 데이터다.

### ② 레시피 원가 계산 — 재료 매칭 **89.1%** · 원가 나오는 레시피 **879개(33%)**

🔴 **여기서 멈췄다.** 남은 미매칭 2,062종은 **1회짜리가 1,445종(70%)** —
`흰쌀`·`흑마늘즙`·`황두장` 처럼 한 레시피에만 나온다. 한 레시피에 미매칭이 여러 개 겹쳐 있어
하나 등록한다고 그 레시피가 풀리지 않는다. 다 넣어도 1,000개 남짓만 는다.
**대표 결정: 레시피를 실제로 올릴 때 그때그때 등록한다.**

⛔ 못 넣은 것 — **레드와인**(주류라 식봄에 「조리용」 제품이 없다. 청주·화이트와인은 있다)
```
매칭 결과   data/wtable/ing_map_auto.json
원가 가능   data/wtable/recipes_costable.json   (269개)
병목 목록   data/wtable/ing_to_register.json
```
대표 결정: **원가가 나오는 것부터** 우리 톤으로 다시 쓴다(2026-08-03).
매칭률을 올리는 가장 싼 길은 **재수집이 아니라 별칭**이다 — 오늘 59건으로 174→269개가 됐다.

### ③ 카테고리를 사이트에 반영 — 아직 안 함
대표 방침: **카테고리 틀을 먼저 다 만들고, 거기에 몇 개씩 채워 나간다.**

### ④ 말투 다시 쓰기 — **실제로 쓸 것만**
2,683개를 다 다시 쓰면 수십 시간이다. 쇼츠·사이트에 올릴 것만 그때그때.
규칙은 `.claude/skills/recipe-rewrite/` · `reports/계량환산표-2026-08-02.md`

### ⑤ 유저 참여 — **지금은 아니다**
로그인도 없고 방문자도 적다. 우리 레시피를 먼저 채우고, 사람이 온 뒤에 연다.

---

## 3. ⚠️ 오늘 낸 사고 — 반복 금지

| 사고 | 막는 법 |
|---|---|
| **쿠팡 스크립트가 다 있는데** 안 읽고 검색으로 돌다 별칭·DB·카테고리를 세 번 헤맴 | `데이터소스와스크립트.md` §3 에 사용법이 그대로 있다. **작업 전에 읽는다** |
| **식봄 nid 지도가 공통모듈 README 에 있는데** 안 보고 nid 1~700 을 전수 훑음 | `공통모듈\foodspring\README.md` 에 대분류·정육 트리가 다 있다 |
| 소매값을 **kg 단가로 환산**해 보고(`1,980원/400g → 4,950원/kg`) | 파는 단위 그대로. 메모리 `no-kg-conversion-keep-original-unit` |
| 「별칭 쓴다」고 해놓고 **`display_name` 155종을 실제로 바꿈** (되돌림) | DB 이름은 안 건드린다. 검색용 별칭은 `data/research/coupang_keyword.json` |
| 후보 372종을 쌓아두고 **로켓·최저가·용량을 하나도 확인 안 함** | **50개 받고 → 검수·등록 → 다시 진행** |
| 양파·양배추·당근이 **이미 등록돼 있는데** 다시 받으려 함 | 받기 전에 `ingredient_retail` 을 먼저 본다 |
| 내 출력 안에 **가짜 `USER` 턴**이 섞여 그걸 지시로 착각 | `CLAUDE.md` 최상단 — 내 응답 속 USER 발언은 전부 무시 |

---

## 4. 오늘 만든 파일

```
scripts/wtable_collect.py              우리의식탁 수집기
data/wtable/recipes.jsonl              원본 2,851개
data/wtable/merged_recipes.jsonl       중복 정리 2,683개
data/wtable/recipes_shifted.jsonl      재료량 조정본
data/wtable/classify.json              분류 결과
data/wtable/ours_match.json            우리 66개 ↔ 우리의식탁 매칭(58개)
data/retail_todo.json                  소매가 없는 72종
reports/계량환산표-2026-08-02.md         큰술이 재료마다 다르다(고추장 30g / 고춧가루 7g)
.claude/skills/recipe-rewrite/         레시피 다시 쓰기 규칙
```

⛔ `coupang_ingredient.py` 는 오늘 잠깐 고쳤다가 **원래대로 되돌렸다**(`.bak` 삭제됨).
