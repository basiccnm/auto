# 세션 정리 — 2026-08-02 (코워크)

이 문서 하나로 이 세션의 결정사항·진행상태를 다음 세션이 이어받을 수 있다.

## 1. Claude Code 안전장치 (사고 재발 방지)

배경: 메모리 지시에도 불구하고 코드탭이 렌더·유튜브 업로드를 무단 실행, DB를 세션마다 재생성하는 사고 반복.

원인과 해법:
- 메모리/지시문은 권고일 뿐 → 기술적 차단 필요
- `.claude/settings.json` permissions **ask** 규칙: `Bash(*upload*)`, `Bash(*youtube*)`, `Bash(*remotion render*)`, DB 경로 Edit/Write, `Bash(*migrate*)`, `Bash(*DROP*)`, `Bash(*ALTER*)`
- 자동 승인 모드(`--dangerously-skip-permissions`) 금지 — 켜져 있으면 ask 규칙 무력화
- CLAUDE.md 절대규칙: DB 스키마 확정·수정 금지, 상태는 CLAUDE.md/STATUS.md만 신뢰, 낡은 TODO 문서는 _archive로 이동
- 세션은 기억이 없다 → 결정사항은 즉시 파일에 기록
- UI 수정 완료 보고 전 스크린샷 자가검수 필수 (헛보고 방지 — 실제로 home-D 헛보고 사건 있었음)

## 2. 스킬 라이브러리 (설치 완료)

- 위치: PC `바탕화면\블로그수입관련\skill\skills-library.zip` (46개 스킬, 전부 저장소 실체 검증 완료)
- 구성: design/ (frontend-design, canvas-design, impeccable, nothing-design, design-auditor) · olmanama/ (claude-seo 25종) · shorts/ (remotion 11종, watch, video_toolkit, hook-generator, youtube-thumbnail, reels-scripting)
- 배치 방식: 프로젝트별 `.claude\skills\` 복사 (폴더 격리 → 오발동 방지). 전역 스킬은 `C:\Users\hardb\.claude\skills\`
- 전역 스킬 2개: **handoff**(인수인계 문서 작성) · **resume-work**(이어서) — `skill\global-skills\`에 있음, 전역 복사는 코드탭이 수행
- watch 스킬 = "Claude 눈": FFmpeg 프레임 추출+자막/Whisper. 클립 선별·렌더 검수용. FFmpeg·yt-dlp 필요
- design-extract는 저장소 사망, npm `designlang`으로 생존 (`npx designlang <url>` — 사이트 디자인 추출)
- 계획: 깃허브 비공개 저장소에 push해서 코워크·코드탭 공용 창고화 (미실행)

## 3. 에듀싱크 (우리아이학교정보)

- 정체성: 학교 정보 조회 앱이 아니라 "아이 학교생활 + 가족 일정 통합 관리". 서랍(6년 기록)·미션이 앱의 진짜 목적
- 차별화: 급식표=알레르기 알림 / 시간표=방과후 포함 고정형 / 일정=학교 연간행사 위에 부모 일정 얹는 다이어리(아이 중심, 부모용 다이어리 확장은 폐기)
- 디자인: 시안 D(토스풍 — 흰 배경, 파랑 #2F6BEA 포인트 하나, 큰 카드 3장, 아이콘 격자, 하단 탭바) 실코드 적용됨. 브랜치 `design/home-concepts`. 테마 6종→2종(밝게/어둡게, 본인 결정). impeccable 감사 완료(:focus-visible, reduced-motion, --outer 토큰 수정)
- **진행 중(1차 지시 발송됨)**: 탭바 홈/일정/미션/서랍/내정보 개편, 미션 홈 주인공화, 급식·시간표는 카드→상세, 동사형 라벨, 전화면 APK → 사장님 폰 검수 대기. 실서버 배포 금지 유지
- **미션 시스템** (핵심 아이템, 80% 구현됨): 일일퀘스트, 판정 3종(즉시/대기=셀카 인증+8시 자동승인/하루끝), 영역 10종, 우리 집 미션. 
- **2차 지시(1차 승인 후)**: 개수 3~5 가변, 보상 상점(부모가 보상 등록 "별 N개=보상", 추천 프리셋, 아이쪽 진행률 표시, 교환 신청→승인→차감, 3일 미승인 넛지), 아이 자율 미션 1칸. 보류: 학원 외부확인 판정, 주간 미션
- 유료 월 2,900원 전환 준비, 실서버 계정 8·자녀 9

## 4. 올마나마 (olmanama.com)

- TTS 사용 중 (초기 자막+BGM 방침에서 변경됨)
- DB/홈페이지 2개 → 1개 통합 예정. 순서: 백업(직접 확인) → 통합 계획 문서 승인 → 실행 → 검증 → 그다음 리디자인. URL 리다이렉트(SEO) 주의
- 스킬 풀세트 설치 지시 발송됨: claude-seo 25종 + shorts 전부 + frontend-design·impeccable·canvas-design + 마케팅팩 content-strategy·analytics·social·ad-creative (마케팅팩 seo류는 중복이라 제외) + charlie947 niche-research·post-scorer
- SEO 키워드 분석 지시 발송됨 (API 연결돼 있음): 서치콘솔 3개월 + 네이버 검색량(메뉴×원가/레시피) + 유튜브 쇼츠 패턴 → seo-cluster → "다음 레시피+쇼츠 소재 TOP 20" 리포트 → reports\ 저장. 결과로 레시피 등록 순서 결정 예정
- 쇼츠는 Remotion으로 통일 권고 (쇼핑쇼츠와 부품 공유)
- 컨설팅 대기 중 (2026-08-02 기준)

## 5. 쇼핑쇼츠 프로젝트

- 소스: 도우인·샤오홍슈 인기(판매량 랭킹 기준) 영상 → 쿠팡 상품 매칭 확인 후 진행
- 여러 영상 컷 조합 (통짜 X). 템플릿: 후킹 1컷 + 시연 3~4컷 + CTA 카드
- 파이프라인: 수집 수동(10~20분/일) → 장면 분리 → watch로 클립 선별(초기 사용자 비교 튜닝) → 정규화 → 자막+TTS+BGM 조립 → Remotion 렌더 → 승인 후 업로드
- 원칙: 틱톡에 도우인 소스 금지, 가공 필수, 채널 소모품, 3주 30개 손절선, 렌더·업로드 승인 게이트
- 상태: 설계 논의 완료, 구축 지시문은 미발송 (에듀싱크·올마나마 작업 이후)

## 6. 에듀싱크 다음 액션

1. 1차 APK 폰 검수 → 승인 → 2차(미션 확장) 지시
2. 올마나마: 스킬 설치·SEO 리포트 확인 → TOP 20 같이 검토 → DB 통합 계획 승인
3. 쇼핑쇼츠: 위 작업 정리되면 파이프라인 구축 지시문 작성
