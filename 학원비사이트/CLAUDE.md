# 우리아이학원정보

> ## 🚨 배포 대상
> **워커 `academy-site-renderer` (사이트) / `academy-tipoff-api` (제보 API) — 커스텀 도메인 없음** (workers.dev)
> 배포: `npx wrangler deploy` — **출력의 워커명이 위와 같은지 확인 후 진행.** 워커가 둘이라 config를 헷갈리지 말 것.
> ⚠️ `olmanama.com`은 여기가 아니다(그건 `..\올마나마`). 2026-07-21 폴더를 헛짚어 오배포한 사고가 있었다.
> ⚠️ **웹 배포는 사장님 "배포해" 필수.** 로컬(wrangler dev)은 승인 불필요.
>
> **로컬 서버: 포트 8787** — `npx wrangler dev --port 8787`
> (다른 프로젝트 포트를 빌려 쓰지 말 것: 에듀싱크 8788 · 올마나마 8801 · 방구석약국 8899 · 베이직씨엔엠 8793)
>
> ⚠️ `_site\`는 11ty 빌드 결과물(8만 개)이라 백업에서 제외돼 있다. 소스에서 재생성되므로 지워도 무방.

작업 폴더: `C:\Users\hardb\Desktop\블로그수입관련\학원비사이트`

## 이 프로젝트 작업 방식

- 화면 만들 때: **360px 우선**, 글자는 `clamp()`로 상·하한 고정, 실제 스크린샷으로 눈 검수
- 공공기관 API가 필요하면 `..\공통모듈\공공데이터\CLAUDE.md`(사용 대장) 확인 후 **복사해서** 사용

전 프로젝트 공통 원칙(규칙 0·목업 먼저·API 물어보기)은 글로벌 `~\.claude\CLAUDE.md`에서 자동으로 읽힌다.
이 프로젝트의 상세 맥락은 메모리에 있다.

## 메모리 씨앗 (첫 세션에서 1회 처리 후 이 섹션 삭제)

`.claude-memory-seed\` 폴더가 있으면 **먼저 처리할 것**: 그 안의 `*.md`(README.md 제외)를
이 세션의 메모리 폴더로 복사 → 씨앗 폴더 삭제 → 이 섹션 삭제.
자세한 건 `.claude-memory-seed\README.md` 참고. 이 프로젝트의 과거 맥락이 거기 들어 있다.
