-- 동의 방식 컬럼 — 2026-07-19 (회신3 지침)
-- 만14세 미만 확인 수단이 무엇으로 정해지든(문자·이메일·카드·본인인증) 동의 UI 컴포넌트에
-- 한 단계 끼워 넣는 구조로 가기 위해, 어떤 방식으로 받았는지를 행마다 남긴다.
-- 지금은 전부 'checkbox'. 나중에 방식이 바뀌어도 **소급 구분이 가능**해진다
-- (예: 법령 해석이 바뀌어 재동의를 받아야 할 때 대상자를 정확히 골라낼 수 있음).
ALTER TABLE children ADD COLUMN consent_method TEXT;
