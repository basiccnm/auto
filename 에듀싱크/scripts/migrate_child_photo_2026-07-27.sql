-- 자녀 프로필 사진 (2026-07-27)
--
-- 왜: 지금은 동물 이모지 8개 중 하나를 고른다. 형제가 둘이면 «🦊 첫째 / 🐰 둘째»인데
--     아이 얼굴을 두면 화면 어디서든 누구 것인지 바로 안다(상단 칩·서랍·홈).
--
-- 어디에: R2 에 둔다. D1 에는 키만. 기록 사진과 같은 규칙이다.
--     키 형식 `child/{id}/{uuid}` — 자녀 단위로 지울 때 prefix 로 훑을 수 있다.
--     썸네일은 안 만든다. 프로필은 원래 작게 쓰므로 480px 한 장이면 충분하다.
ALTER TABLE children ADD COLUMN photo_key TEXT;
