# 우리아이학교정보 (구 에듀싱크)

> ## 🔇 반복 금지 — "네이버 API 7일 남았다"
> **사실이 맞다. 사용자도 이미 알고 있고, 다른 방법으로 대응 중이다(2026-07-24).**
> 그러니 **다시 꺼내지 마라.** 알고 있는 사람에게 같은 경고를 반복하는 것은 도움이 아니라 소음이다.
>
> - 이미 통보한 사안은 **한 번만** 말한다. 턴마다·작업마다 반복 금지
> - 만료·한도·비용 경고는 **실제로 에러가 났을 때** 또는 **사용자가 물었을 때**만
> - 상태가 **바뀌었을 때**(실제 만료됨·차단됨)는 보고할 것 — 그건 새 정보다

> ## 🔇 반복 금지 — ".dev.vars 시크릿 노출" (2026-08-07 종결)
> **커밋 `a71d75b`부터 퍼블릭 레포 히스토리에 `.dev.vars`(AUTH_SECRET·ADMIN_PASSWORD·
> 카카오/구글 실키·NEIS 키)가 올라가 있었다. 이미 조사·판단·조치 전부 끝났다.**
>
> | 확인한 것 | 결과 |
> |---|---|
> | D1 전수 대조 (계정·기기·관리자조작·토큰폐기) | 노출 이후 신규 계정 0 · 낯선 기기 0 · 관리자 조작 0(최종 07-11) — **악용 흔적 없음** |
> | 히스토리 정리 | `git filter-repo` 로 **완전 제거 완료**(08-07, 45.6MB) |
> | 키 재발급 | **대표 판단으로 보류 확정** — 위 확인 결과 근거로 재발급 없이 간다 |
>
> **그러니 다시 꺼내지 마라.** 이미 아는 사람에게 같은 경고를 반복하는 건 도움이 아니라 소음이다.
> - 재발급 여부는 **종결된 결정**이다 — 재차 권유하거나 되묻지 말 것
> - 다시 볼 필요가 생기는 경우는 딱 하나 — **실제 악용 정황이 새로 발견됐을 때**뿐이다

> ## 🚨 배포 대상
> **워커 `eduthink-site-renderer` / 커스텀 도메인 없음** (workers.dev)
> 배포: `npx wrangler deploy` — **출력의 워커명이 위와 같은지 확인 후 진행.**
> ⚠️ `olmanama.com`은 여기가 아니다(그건 `..\올마나마`). 2026-07-21 폴더를 헛짚어 오배포한 사고가 있었다.
> ⚠️ **웹 배포는 사장님 "배포해" 필수.** 로컬(wrangler dev)은 승인 불필요.
>
> **로컬 서버: 포트 8788** — `npx wrangler dev --port 8788 --local`
> (다른 프로젝트 포트를 빌려 쓰지 말 것: 학원비 8787 · 올마나마 8801 · 방구석약국 8899 · 베이직씨엔엠 8793)
>
> 커스텀 도메인을 붙이면: **카카오·구글 콜백 URL 재등록** + `templates.js`의 `SITE_ORIGIN` 교체 (안 하면 로그인·공유 미리보기가 깨진다)

> ## 🚨 출시 전 반드시 내릴 것 — `DEV_TOOLS = "true"`
> `workers\site-renderer\wrangler.toml` 42행. **실서버에 켜진 채로 나가 있다.**
> 결제 테스트 때문에 일부러 켜둔 것이라 지금은 그대로 두지만, **출시 전 `"false"` + 재배포**.
> 켜져 있는 동안 실서버에서 **`mock` 결제가 통해 공짜 이용권이 발급되고** `/dev/login-as` 가 열린다.

작업 폴더: `C:\Users\hardb\Desktop\블로그수입관련\에듀싱크`

## 🎨 필수 스킬 규칙 (2026-08-02 설치)

스킬은 `.claude\skills\` 에 있다. **이 프로젝트에서만** 뜬다.

- **UI를 만들거나 고칠 때는 `frontend-design` 을 먼저 읽고 적용한다.** 색·서체·간격을
  즉흥으로 정하지 말 것 — 미학 방향을 먼저 확정하고 그 다음에 코드를 쓴다.
- **화면을 내놓기 전에 `design-auditor` 로 감사한다.** 대비·터치영역·다크모드·접근성.
  아래 «360px 우선» 원칙과 같이 본다.
- 크게 뜯어고칠 때는 `/impeccable` 을 직접 호출한다.
- `nothing-design`·`canvas-design` 은 **명시적으로 요청할 때만** 쓴다(학부모 앱 UI용이 아니다 —
  각각 모노크롬 스타일용·포스터/PNG 아트용).

## 📸 검수 보고 규칙 (2026-08-02 상시)

> ### ✅ 드라이브 사용 재개 (2026-08-03, 사장님 지시)
> 2026-08-02 저녁의 드라이브 중지 지시는 해제됐다. 아래 지시서·업로드 규칙 그대로 쓴다.

- **스크린샷 개별 전송 금지.** 전체 화면을 **몽타주(격자 합성)**로 만들어
  **라이트 1장 + 다크 1장, 총 2장**으로 합쳐 보고서와 함께 한 번에 첨부한다.
- **범위 잠금(2026-08-02):** 구현은 **지시서 항목 + 대표님이 채팅으로 직접 지시한 버그 수정**만.
  작업 중 떠오른 개선·신규 기능은 구현하지 말고 검수 폴더 `제안-보류.md` 에 한 줄씩 적는다
  (컨설팅 세션이 회수). 회신서에는 **「직접 지시분」 구획**을 두고 채팅 직접 지시 처리분을 구분 기재.
- **지시서(2026-08-02 갱신):** 컨설팅 세션이 `G:\내 드라이브\에듀싱크-검수\` 에
  `지시서-MMDD-N.md` 로 내린다. **새 배치 시작 전 폴더에서 가장 최신 지시서를 읽을 것.**
  옛 `지시서.md` 는 완료된 배치 — 재실행 금지. 보고서는 같은 폴더에 `보고-MMDD-N.md`.
- **드라이브 업로드(확정 2026-08-02): 구글드라이브 데스크톱 `G:` 복사 방식.**
  `G:\내 드라이브\에듀싱크-검수\` 에 `yyyyMMdd-HHmm-<이름>.png` 로 복사한다
  (PowerShell Copy-Item — PNG 바이너리라 MCP 업로드보다 확실하다).
  ⚠ `프로젝트백업\` 안에는 절대 두지 말 것 — 21:00 미러가 지운다. 검수 폴더는 미러 밖이라 안전.

## 이 프로젝트 작업 방식

- 협업 규약 정본: **`docs\운영규칙-마스터.md`** (전략세션=웹 ↔ 코드탭 우체통 방식, `요청N`/`회신N`)
- 세션 인계: `docs\인계-코드탭.md`
- 진행 중: **블록1 자녀기록** (지시서 `docs\블록1-지시서-v5-통합.md`)
- 화면 만들 때: **360px 우선**, 글자는 `clamp()`로 상·하한 고정, 실제 스크린샷으로 눈 검수

전 프로젝트 공통 원칙(규칙 0·목업 먼저·API 물어보기)은 글로벌 `~\.claude\CLAUDE.md`에서 자동으로 읽힌다.
이 프로젝트의 상세 맥락은 메모리에 있다.
