# 아이서랍(구 에듀싱크·우리아이학교정보) — 현재 상태 (STATUS)

> 🔴 **상태는 이 문서만 신뢰한다.** 다른 문서와 충돌하면 이것이 이긴다.
> - **지시서 정본**: `docs/지시서-0807-MVP재설계.md` (드라이브 원본 사본, 2026-08-07)
> - **08-03 이전 상세**(테마 색 4차·홈 v4·§1~§7 개편 함정 기록)는
>   `docs/_archive/STATUS-20260803.md` 로 넘겼다. 「왜 코드가 이렇게 생겼나」가 궁금할 때만 연다.
>
> 최종 갱신 **2026-08-07 (§6.1 카드 80% · §6.2 시계 개편 반영)**

## ① 지금 상태

- **`master` = 실서버 배포본.** 워커 `eduthink-site-renderer`(2026-08-03 배포)
- **앱 이름 「아이서랍」** — 표시명 전부 교체됨. ⚠ 패키지 `com.eduthink.app` 은 **영구 불변**
  (바꾸면 기존 설치본과 다른 앱이 되고 로그인 콜백·푸시 토큰이 끊긴다)
- **화면은 홈 v4 + 테마 18종 × 라이트/다크 36조합** 까지 완성. 일러스트 대기 **0**
- **워커 소스가 08-07에 git 으로 편입됐다.** 그 전엔 한 번도 커밋된 적이 없어
  파일이 사라지면 드라이브 미러가 유일한 사본이었다

### 지시서 0807 대비 진행표

| § | 항목 | 서버 | 앱 화면 |
|:-:|---|:-:|:-:|
| §8 | DB 표 6개 + `orders` 확장 | ✅ 작성·검증 · **⛔ 실서버 미적용** | — |
| §8+ | PIN 시도 제한 컬럼 2개(`migrate_pin_2026-08-07`) | ✅ 작성·검증 · **⛔ 실서버 미적용** | — |
| §3 | 2단계 보상 검증(1차 즉시·2차 보너스) | ✅ API | ✅ **배선 완료** |
| §4③ | 별도장 상점(진열대·바꾸기·티켓) | ✅ API | ✅ **배선 완료** |
| §4⑤ | 칭찬 리액션 | ✅ API | ✅ **배선 완료**(미션 화면 안) |
| §4② | 하이브리드 미션 세트 + 🎲 리롤 | ✅ **리롤 API + 하루 1회 서버 판정** | ✅ **배선 완료** |
| §4④ | 아이 모드 + 4자리 PIN | ✅ **API 3개 + 시도 제한** | ✅ **배선 완료** |
| §0-3 | 요금제 6,900/12,900 | 🔴 **옛 차등가 그대로**(2,900/7,900/14,900/25,000) | 🔴 |
| §4① | 홈 합성(전광판+미션 세트+진열대) | ✅ 재료 다 있음 | ✅ **배선 완료** |
| §7 | PC 1024px+ 3컬럼 대시보드 | — | 🔴 미착수 |
| §1.2 | 100% 무광고 | 확인 안 함 | 확인 안 함 |
| §7 | 출시 전 점검(푸터 법정정보·IAP·`DEV_TOOLS`) | 🔴 미착수 | 🔴 |

> ✅ **보상·PIN 배선 완료(2026-08-07).** 화면이 서버와 실제로 왕복한다.
> 검사는 `scripts/qa/` 의 3종 (wrangler 없이 돈다) — reward 8/8 · pin 8/8 · reroll+react 7/7.
> 이전 기록 ↓
> 🟡 **화면 목업이 붙었다(2026-08-07)** — #storeset·#store·#storebuy·미션 두 구역·아이모드+PIN.
> 더미 데이터로 눌러볼 수 있고 **서버엔 안 나간다.** 각 App 메서드에 `api(...)` 주석이 있어
> 배선할 때 그 줄만 바꾸면 된다. 말 규칙(티켓·바꾸기)은 §1.1 확정표 그대로.

### §6 디자인 확정분 (2026-08-07 저녁)

- **§6.1 카드 = 테마색 80% 농도 확정** — 원색은 쨍하고 65/50%는 물빠짐. 섞는 상대는
  배경이 아니라 **같은 밝기의 무채색 #43464C** (배경을 섞으면 카드가 밝아져 글씨가 죽는다)
- 🔴 **다크는 accent 를 그대로 못 쓴다** — 다크의 accent 는 밝은 파스텔이라 카드가 밝아진다.
  다크 카드는 `--drawer-a`(어두운 파생) 80% 로 갈랐다
- **§6.2 시계 개편** — 회색 알약 박스 폐지, 날짜는 시계 위 독립 줄(14px)·시계 46px.
  글래스모피즘 대신 «제거» 를 택했다(§6 blur 0개 규칙과 충돌). 그늘(.bd::before)이 받친다
  ⚠ 같은 클래스 규칙이 파일 곳곳에 겹겹이다 — `.bd-time` 배경이 §6.2 블록 «뒤»에도 있어서
    한 번에 안 사라졌다. 지울 땐 grep 으로 전부 찾을 것
- **글씨 = 광 죽인 미색** — 순백 대신 흰색에 그 테마 accent 를 섞는다(90/84/82%).
  회색으로 죽이면 18종이 같은 톤으로 수렴한다
- 검증: **대비 실측 18종×라이트/다크 = 36조합 전부 통과**(가로스크롤·JS 에러 0)
- ⚠ 측정은 브라우저가 칠한 픽셀로 — `color-mix` 는 `color(srgb 0~1)` 로 돌아온다.
  255 스케일로 읽으면 대비가 엉뚱하게 나온다(rgba 와 형식이 다르다)

### 파랑·흰색 청산 + 홈 마감 (2026-08-07 밤)

- **파랑 고정 3곳 청산**(대표님 지시) — `.btn-primary`(#2A5FA8 그라데이션 고정) ·
  `.msc.ms-wait`(파랑 고정) · `.msc`/`.ms-mkbtn`(순백 --card). 전부 accent 파생으로.
  ⚠ **색이 «테마를 따르는 것처럼 보여도 고정값일 수 있다»** — 기본 테마의 accent 가
  파랑(#1B4B8F)이라 6일간 아무도 몰랐다. 테마를 2~3개 바꿔 보면 바로 드러난다
- **홈 마감 6건** — ①시계 반투명 받침(그 테마의 art-1 — 흰/회색 받침이 «회색 스티커»의 원인)
  ②☰ 테마 적응 ③우상단·달력 = 카드색 2톤 다운 칩 ④하단 3버튼 통일(★ 광 죽임)
  ⑤바탕 세로 그라데이션 ⑥상단 자녀칩 제거(드로어와 입구 중복)
- **키드 테마 갈래** — applyKidTheme 이 accent 를 --k(밝은 파스텔)로 바꾼다. 다크와 같은 함정.
  아이 화면은 «밝은 파스텔 카드 + 진한 글씨»(기존 아이 문법)로 갈랐다.
  ⚠ 키드 테마를 안 골랐으면 --k 가 빈 값 → :root 폴백(--k=accent 파생)을 깔았다
- **감사기 함정 셋** — ①`color(srgb 0~1)` 스케일 ②그라데이션 배경은 backgroundColor 가
  투명이라 엉뚱한 면과 비교(색 스톱 중간값을 읽어야 한다) ③알파 .92 면은 «불투명»으로 안 잡힌다
- 검증: **상점 흐름 288조합 + 홈 36조합 위반 0건**

## ② 진행 중

- **🔴 실서버 적용은 대표님 PC 에서 해야 한다.** 코드탭 컨테이너에 Cloudflare 자격증명이 없다
  (확인함 — `CLOUDFLARE_API_TOKEN` 없음 · `~/.wrangler/config` 없음 · Cloudflare MCP 401).
  순서·명령·점검표는 **`docs/배포점검-0807-보상시스템.md`** 에 그대로 적어 뒀다.
  A(DB 마이그레이션 2개) → B(워커) → C(앱) 순. 되돌리기도 같이 있다.
- 그 전까지 실기기에서는 보상·PIN·리롤이 안 돈다(표가 없어 서버가 500 을 낸다)

## ③ 다음 할 일

1. ~~모듈 B 앱 화면~~ · ~~아이 모드 PIN~~ — **끝**(2026-08-07 배선까지)
2. ~~§4⑤ 칭찬~~ · ~~§4② 리롤~~ — **끝**(2026-08-07)
3. **실서버 마이그레이션 적용** — 추가만 하는 마이그레이션, 롤백 SQL 준비됨. **승인 필요**
4. §7 PC 대시보드 · §8 출시 점검(푸터 법정정보 · 인앱결제 · 계정 삭제 웹페이지)
5. **요금제 6,900/12,900** (§0-3) — 값만 바꾸면 되니 뒤로(대표님 판단 2026-08-07).
   체험 기간도 같이 정한다(지시서 §1.3=1주 / 현재 코드=2주)
6. **스토어 등록정보** — 설명·스크린샷·그래픽(아이콘은 준비됨) · 인앱결제 상품 등록

## ④ 금지사항

- 배포 대상은 워커 **`eduthink-site-renderer`** (olmanama.com 아님 — 07-21 오배포 사고)
- 웹 배포는 대표님 **"배포해"** 승인 필수. 로컬(`wrangler dev --port 8788`)은 승인 불필요
- **남의 앱 APK 디컴파일 금지** — 화면 보고 배우는 벤치마킹까지만
- 보상 설계 원칙: **앱이 보상을 주지 않는다** (부모가 부모 앱에서 등록·수정)
- 스크린샷 개별 전송 금지 — **몽타주 라이트 1 + 다크 1**
- ⛔ **말이 끝나기 전에 코드부터 돌리지 말 것** (2026-08-03 반복 지적)
- 범위 잠금: **지시서 항목 + 채팅 직접 지시**만. 떠오른 개선은 `docs/제안-보류.md` 에 한 줄

## ⑤ 🚨 출시 전 반드시 내릴 것

- **`workers/site-renderer/wrangler.toml` 42행 `DEV_TOOLS = "true"`** → `"false"` + 재배포
  - 대표 확인: **결제 테스트 때문에 일부러 켜둔 것** — 지금은 그대로 둔다
  - 켜져 있는 동안 실서버에서 **`mock` 결제로 공짜 이용권이 발급**되고 `/dev/login-as` 가 열린다
- **개인정보처리방침에 「나이대」 수집을 올릴 것** — 온보딩 설문에서 받는다.
  스토어 「데이터 보안」 양식에도 필수. 빠뜨리면 최소수집 원칙 위반
- **계정 삭제 웹페이지**를 워커에 만들어야 한다 (구글 요구사항, 아직 없음)
- ⚠ 타겟 연령에 **13세 미만**이 들어가면 구글 **Families 정책** 심사 — 반려가 잦은 자리다

## ⑥ 함정 (다시 밟지 말 것)

- **`index.js` 접두어 화이트리스트** — `api_*.js` 에 라우트를 열어도 `path.startsWith(...)` 목록에
  없으면 **조용히 HTML 404 로 샌다.** (보상 API 는 `index.js:2598` 에 등록 완료)
- **잔액은 `star_ledger` 하나로만 센다.** `balance` 컬럼을 따로 두면 두 값이 반드시 어긋나고,
  그때부터 어느 쪽이 맞는지 알 수 없다
- **`created_at` 은 UTC 인데 비교는 KST 로 하면 9시간 어긋난다** — 중복 방지가 통째로 안 걸려
  같은 미션을 같은 날 두 번 내면 도장이 두 번 나갔다. `date(created_at,'+9 hours')` 로 DB 안에서 맞춘다
- **`child_id` 는 INTEGER** (`children.id` 가 INTEGER). TEXT 로 넘기면 에러가 아니라
  **«0건 일치»로 조용히 지나간다**
- **APK 빌드** — `JAVA_HOME` 을 Android Studio JBR 로 잡아야 한다(기본 Java 8 로는 실패).
  APK 는 프로젝트 밖 `%LOCALAPPDATA%\eduthink-build\...\apk\debug\` 에 떨어진다
- **`.wrangler/` 를 커밋하면 로컬 D1 이 깨진다** — `.sqlite` 본체와 `-shm`/`-wal` 이 어긋나
  «database disk image is malformed» 가 난다. 지금은 무시 처리됨
