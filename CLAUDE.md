# 블로그수입관련 — 전 프로젝트 공통 규칙

> 이 폴더 아래 **모든 프로젝트**(에듀싱크·올마나마·쇼핑쇼츠·학원비사이트 등)의
> 코드탭 세션에 자동 적용된다. 프로젝트별 CLAUDE.md보다 먼저 읽힌다.
> (작성: 웹 전략세션 2026-08-02)

## 상태 동기화 (절대규칙)

- **세션 시작**: 어떤 작업이든 하기 전에 프로젝트의 `STATUS.md`를 읽고
  "현재 상태 3줄 + 이번 세션 할 일"을 먼저 보고한다. 없으면 만들고 시작한다.
- **STATUS.md만이 진실이다.** 다른 TODO·계획·핸드오프 문서와 충돌하면
  STATUS.md를 따르고, 낡은 문서는 `_archive\`로 옮긴다.
- **상태가 바뀌는 즉시**(커밋·빌드·배포·지시 완료) 같은 턴에 STATUS.md를 갱신한다.
  세션 끝에 몰아서 하지 않는다.
- **세션 종료·전환 시** `handoff` 스킬 실행, 새 세션은 `resume-work`로 시작한다.
- STATUS.md 형식: 최상단 `[기준일시]` + ①지금 상태 ②진행 중 ③다음 할 일 ④금지사항.
  50줄 넘으면 오래된 항목은 `_archive\STATUS-YYYYMMDD.md`로 넘긴다.

## 보관·정리

- `_archive\` 문서는 **90일 경과 시** handoff 실행 때 자동 삭제한다.
  삭제 전 파일명 목록만 보고에 한 줄 남긴다.
- **DB·코드 백업은 자동 삭제 금지.** 최근 5개 초과분은 "삭제 후보 목록"으로
  보고만 하고, 승인받은 뒤에만 삭제한다.
- 배포 직전 백업은 개수와 무관하게 **다음 배포가 성공 확인될 때까지** 보관한다.

## 검수 보고 (전 프로젝트 공통)

- 스크린샷 **개별 전송 금지** — 몽타주(격자 합성)로 라이트 1장 + 다크 1장,
  보고서와 함께 한 번에 첨부한다.
- UI 완료 보고 전 스크린샷 자가검수 필수.


